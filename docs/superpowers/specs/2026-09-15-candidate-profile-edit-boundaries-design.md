# 候选人资料编辑边界、私有筛选要求与城市名称统一

状态：已批准。2026-09-15 用户确认最终范围并要求进入 development-workflow 计划阶段；本会话不实施产品代码。

校准基线：`2312cba5a68f8c2156872dadd0633230330cd4b0`（origin/main）；规划分支：`fix-resume-edit-onboarding-screening`。文件路径均相对仓库根目录。

## 1. 目标与已确认范围

候选人完成 onboarding 后，从“我的简历”编辑资料，只修改简历并回到简历；从“求职意向”编辑筛选要求，直接编辑首次引导拼接后保存的私有文本；城市选择的热门区、分省列表、搜索与已选项对同一城市显示一致名称。

用户已确认：历史结构化排除字段没有消费者，本次保留，不增加迁移、清理或生效排查。此项作为任务边界，不代表本轮重新审计了后端消费者。

首次 onboarding 的勾选方式保留。后续新增／编辑意向共用表单内的“哪些情况直接排除？”替换为文本框。本次不修改后端接口。

## 2. 现状证据

- `src/屏幕/我的.tsx` 已将“我的简历”和“求职意向”拆成两个入口。
- `src/屏幕/我的简历.tsx` 的字段、经历条目、完整度补全和身份缺失提示均可能进入共用资料页。
- `src/屏幕/基本信息.tsx` 保存后仍前往学历或求职状态；`src/屏幕/工作经历.tsx` 保存后仍前往求职状态或引导问答，未按简历编辑入口终止。
- `src/屏幕/求职状态.tsx` 保存后还派发到岗预填并进入学历／向导；该屏也是简历缺失身份的补全路径。
- `src/屏幕/引导问答.tsx` 的后半段包含偏好题，最后保存个人优势并调用保存首次意向，因此日常简历编辑不得经它收尾。
- `src/数据/后端映射.ts` 的 `转首次意向写入` 在前端将固定卡片转换成“不接受…”文案，与自定义原文按换行拼接后提交 `private_preferences`；结构化 exclusions 四项为 unspecified。
- `从BFF意向草稿` 已读取完整 private_preferences；`转意向写入` 已支持写回空字符串。现有后续表单却把文本拆行当卡片，并允许固定卡写 exclusions。
- `src/屏幕/发布岗位.tsx` 已有“给 AI 代理的筛选要求 / 选填”标签、说明与 textarea；其 200 字限制不作为候选意向的契约。
- 城市旧短名问题已在校准基线实现修复：`src/数据/城市精选.ts` 保存 22 个规范名并提供有限兼容映射，四处消费者已接线；本任务不再实施这部分。

上述为源码核对与用户截图证据，尚未执行真实浏览器复现或验证后端运行态。

## 3. 方案取舍

采用现有共用资料页加显式编辑来源、替换意向表单局部控件、统一城市展示名的方案。它直接修正导航与展示边界，不新增一套页面或持久化模型。

不采用复制独立简历编辑器：会重复资料校验和保存链。也不采用仅由引导草稿是否为空推断来源：Mock／Backend、刷新和恢复存在不同状态，来源应可直接确定。城市不采用点击时逐个搜索或重写全部目录名字：已有 canonical ID 足以维持选择身份，差异在名称。

## 4. 简历编辑契约

### 4.1 入口与模式

从“我的简历”进入基本信息、工作经历及求职状态时，使用显式 URL 参数 `from=resume`。覆盖普通字段入口、工作／教育／技能／项目／证书入口、完整度补全入口和身份缺失后的补全入口。参数能保留到刷新，不由临时全局状态推导。

只有上述共用资料页识别该参数；无参数的首次 onboarding 路线保持原行为。若基本信息因身份为空必须继续到求职状态，须继续携带该参数。

### 4.2 保存、返回与数据

- 编辑模式：基本信息的主按钮显示“保存”；工作经历继续使用“保存”；求职状态补全的完成动作显示“保存”。各屏保存成功回到“我的简历”，不得进入意向题、披露说明或头像注册流程。
- 工作经历页内部的经历／教育编辑保存仍回到本页列表，整页保存才结束本次编辑。
- 基本信息身份为空时保留既有资料补全要求：先进入带编辑参数的求职状态，成功保存资料后回简历，不绕过必填要求，也不继续学历与偏好流程。
- 编辑模式不得创建或更新首次意向，不派发到岗引导预填，不写 onboarding 建档草稿、不确认 onboarding 预填分区、不触发 onboarding 建议恢复。必要时调整现有预填位置判定对 `from=resume` 的识别；不重构整个恢复机制。
- 继续使用现有简历保存操作和权威回显。保存失败保留输入并显示既有错误，不返回简历、不显示成功。
- 返回／取消沿用既有导航及页面草稿语义；本次不新增全局未保存拦截。通过明确成功落点结束编辑，不要求用户手动退出向导。
- 简历内显式“添加意向”的独立入口不携带简历编辑参数，不属于简历保存的后续步骤。

### 4.3 首次引导回归

学生、社招无参数入口继续原有资料、偏好、个人优势、披露与完成步骤，保留恢复和解析预填能力；不能通过“所有保存都回简历”修复日常编辑。

## 5. 意向私有筛选要求契约

在 `src/屏幕/添加意向.tsx` 的新增／编辑共用表单中删除排除卡片区域，原位置替换为与招聘侧视觉一致的多行输入：

- 标签：“给 AI 代理的筛选要求”；旁标：“选填”。
- 说明：“写下你的要求和偏好，AI 代理会据此筛选岗位。”
- 完整回显当前意向的 `private_preferences`，直接绑定 `意向草稿.私有偏好`，沿用现有草稿与保存链。
- 可编辑、增删任意行及清空全文。空值显式提交 `''`，不能转为 undefined 后回退原值。保留换行及原文，不拆成卡片，不二次加“不接受”前缀，不进行模型改写。
- 不照搬招聘侧 200 字输入限制，不截断已有文本；沿用意向接口的既有校验和错误提示，不新增未经后端契约支持的限制。
- 历史 exclusions 原值透传，新建保持现有 unspecified 默认值。私有文本修改不联动清除或生成结构化排除值。
- 不改变工资、办公方式、招聘类型、目录引用等其他意向字段。继续使用带 revision 的既有更新请求与保存后回显机制；失败不离页。

复用招聘侧的布局、文案结构和样式原则即可，不为了这一块引入通用表单框架。首次 onboarding 的排除卡及其固定拼接规则不受控件替换影响。

## 6. 城市名称修复已合入，保留回归责任

在校准基线 `2312cba5` 中已核对实际代码：12 个国内热门项使用北京市／上海市等目录名，10 个海外项使用目录标准名称；`规范精选城市名称` 与 `精选城市显示名` 提供有限旧名兼容。`选工作城市.tsx`、`选择城市.tsx`、`引导问答.tsx`、`发布岗位.tsx` 已消费该规则，Mock 也按有限映射兼容旧草稿与搜索。

采用已合入 `docs/superpowers/specs/2026-09-15-editor-catalog-fullscreen-design.md` §5.6 的契约，不恢复早期“海外保持中文别名／Mock 不变”的旧规则，不新增一轮城市实现或数据核对。保存按 ID、去重、单多选上限及取消不落草稿继续保持。

本任务修改引导问答时，只回归其已有城市消费者；不重复改城市配置、名称映射或选择器结构。本轮结论是源码已合入，非本轮浏览器验收 PASS。

## 7. 技能与资格证书组件统一

用户追加：onboarding 和简历编辑中的专业技能、资格证书均采用当前专业技能的组件形式。两种旅程本来共用 `src/屏幕/工作经历.tsx`，差异在该页内技能是标签卡、证书是列表行；不另做两套实现。

- 将技能与证书统一为标签展示、输入框、添加按钮和逐项删除的同一组件。标签支持换行排列，长名称不撑破页面；技能与证书保留各自标题、输入提示和可访问删除名称。
- 共用组件仅承担展示与输入交互；技能仍为字符串列表，证书仍为包含编号、名称、年份的对象列表，不把证书降为字符串或重建已有 ID。
- 既有证书年份不丢失，有年份时仍可见；新证书仍按既有规则年份为空，不增加年份输入。
- 保留既有添加、空白校验及去重语义：技能沿用去重；不借样式统一删除历史同名证书或改变证书身份。证书按编号删除。
- 保留 Enter 添加与中文输入法组合输入保护，以及 onboarding 证书未提交输入的草稿恢复和成功添加后清理语义。页面整体保存归属不变。
- 共用组件的两个当前消费者就是技能与证书，不扩展为任意字段表单框架；“我的简历”摘要展示卡不在此组件替换范围。

## 8. 个人优势编辑入口

当前 `src/屏幕/我的简历.tsx` 的个人优势卡只有普通 div，没有点击事件；`src/屏幕/引导问答.tsx` 已有优势题编辑 UI，但其完成处理同时写个人优势和首次意向，不能直接照搬完成逻辑。

- “我的简历”的个人优势区改为可点击、可键盘操作的编辑入口；已有正文保留换行，空值显示明确的添加入口。
- 复用 onboarding 的个人优势编辑界面，从简历进入时直接显示个人优势，不先经过城市、薪资或排除题。使用显式编辑来源 `from=resume`，刷新仍保持个人优势编辑场景。
- 编辑初值来自当前已加载的简历个人优势；共享编辑正文与既有 500 字规则。不因日常编辑而使用演示文本或恢复 onboarding 解析建议覆盖现值。
- 编辑模式按钮为“保存”，只调用既有 `保存个人优势`；成功回到“我的简历”并显示保存后的值，失败保留输入及既有错误提示。返回而未保存不得提交个人优势。
- 不调用保存首次意向、不确认 onboarding 分区、不写引导草稿，也不进入披露、头像流程。
- 日常编辑不显示“已根据你上传的简历预先提取”或引导恢复动作等不成立的描述；onboarding 的实际预填、恢复与后续流程保持原有语义。
- 优先对既有向导添加窄范围个人优势编辑分支并复用优势题正文，不复制整套编辑器，不改变 onboarding 题序。具体分支入口与保存隔离的测试契约在 Plan 冻结。

## 9. “我的”求职代理卡片展示统一

用户追加：Backend 复用 Mock 代理卡的展示形式，填入实际 MatchCase 数量和规则数，不再向用户显示技术名词“当前 MatchCase”。

当前 `src/屏幕/我的.tsx` 已共用卡片 JSX、样式、代理图标和管理入口，只分支处理状态文案与在线绿点，无需另建卡片组件。

- Backend 状态文案改为“在线 · 正在跟进 N 个机会 · 规则 M 条生效”。保留既有卡片标题、样式与跳转规则库的行为。
- N 沿用当前候选主体的权威 MatchCase summary `openTotal`，经 `取P5Open统计` 投影；表示未结束的 MatchCase 数，不改为分页列表长度、全量历史案件或尚未开案的委托数量。
- M 沿用已水合的生效规则数：全局规则与意向级规则中生效项之和。不是 exclusions 字段数，也不是私有文本行数，不新增规则计数接口。
- 保留当前主体隔离、首载／刷新／失败时 N 显示 `—` 的语义；成功的零显示 0；规则未水合时不显示规则计数，不拿 Mock 或旧主体数据补位。
- 用户明确要求保留“在线”字样与绿点，Backend 与 Mock 同一展示，仅替换真实数字。“在线”按产品固定展示处理，不推导或新增运行时在线检测；覆盖先前禁止 Backend 展示该字样的旧 UI／测试要求。
- 修改 `src/屏幕/我的.test.tsx` 中旧“当前 MatchCase”断言，保留统计精确性、加载失败／主体隔离及规则水合的有效覆盖；不改上方四项统计卡的口径。

## 10. 验收责任

实施 Plan 将下列风险落实到现有测试入口。对尚未实现的五项改动，先验证相关行为会失败，再实现修复；第4条城市行为已由上游实现，本任务不要求它先失败，只按实际改动范围复用或补跑回归：

1. 从简历所有共用页入口进入、刷新、保存，只回简历；学生／社招均覆盖。身份缺失补全后不落向导；保存拒绝留在编辑页；不发生首次意向写入和引导状态写入。
2. 无编辑参数的首次 onboarding 仍能进入偏好阶段并完成，恢复和预填判定不受影响。
3. 首次拼接的多行私有文本完整回显，编辑后保存并重开一致；可清空；超过 200 字的既有文本不丢失；其他意向字段和 exclusions 保留；首次 onboarding 卡片仍存在。
4. 城市同 ID 名称一致、跨区域取消、旧短名回显、主城市禁选与上限等已由基线 `2312cba5` 的现有城市测试覆盖。本任务修改引导问答时回归该消费者；不要求重写或先跑失败，也不无条件重跑两张未改城市页。仅实际修改共享城市配置／选择正文时，按 §6 扩大受影响选集。
5. 对更改的页面做浏览器布局检查，确认文本框可输入多行，标准城市名不会挤压按钮或底部 chips；真实后端意向保存／重新读取与简历保存回跳按 `docs/dogfood/真实后端行为验收.md` 选择相关路径，缺少环境时明确记录未完成，不用 Mock 证明真实持久化。

6. 技能与证书在 onboarding／日常编辑使用同一标签组件；添加、删除、中文输入法 Enter、证书编号／年份保留、未提交证书输入的刷新恢复均有针对性回归。用行为证明数据保留，不只断言相同 className。
7. 个人优势有值／空值均可进入独立编辑场景；刷新不回引导，保存成功回简历，失败留页，取消不写入，首次意向写入次数为零；正常 onboarding 仍保留其个人优势及首次意向完成链。

8. 代理卡用真实 openTotal 展示机会数，规则数按生效及水合状态显示；超过单页数量、成功零、刷新、失败与主体不匹配均不能回退假数或陈旧数；卡片仍可进入规则库。

复用当前 Vitest／Playwright 和真实后端验收路由，不新增测试基础设施。具体最小命令、责任分工与执行模型在获批后的 Plan 冻结。本轮文档规划不运行重测试，也不声明产品验证通过。

## 11. 非目标与延后事项

不迁移或删除 exclusions，不排查其消费者；不改变后端 schema、首次卡片措辞与私有诉求解释；不重构整个 onboarding 状态机；不拆独立简历编辑器；不新增通用 textarea／城市目录框架；不再次变更已合入的海外名称政策；不扩展招聘侧页面功能。

未来仅在出现真实消费冲突证据时另议 exclusions；重复编辑页需求确实超出显式来源可表达范围时才讨论路由重构；出现跨地区目录命名需求时另议全球展示名称策略。以上均不成为本次实施的附加工作。

## 12. 审阅与批准记录

- 2026-09-15：按用户指定 development-workflow 汇总原三个问题；尚未批准。
- 2026-09-15：rebase 至 `4939af8a`；城市由 brown-lion 承接。本任务实施范围收敛为简历编辑结束边界、意向私有文本框、技能与证书组件统一、个人优势编辑入口。新增后两项来自用户明确要求；修订版仍待用户批准。
- 实施前核对 `humble-spider`（`investigate-drawer-background-shift`）的抽屉与日期控件改动，以及 `brown-lion`（`audit-option-components-editors`）的全屏目录编辑改动是否已合入目标基线。保留其组件和测试成果，不覆盖或重复建设；仅有规划文档不能当作已实现。两者会修改工作经历、基本信息、添加意向及相关测试，Plan 应冻结实际可用版本后再安排实施。
- Spec 批准后：规划 owner 实际调用 writing-plans，编写一份零上下文 Plan，按 workflow 调用 Claude 异构文档 review；review 结果写入 Plan，不新增独立报告。完成后交付两个宿主的新实施会话提示词。

- 2026-09-15：追加求职代理卡片展示统一，沿用现有 MatchCase summary 与规则计数，本任务共五项产品修复；城市继续由外部分支承接。

- 2026-09-15：用户明确保留“在线”与 Mock 同款展示，并以“就这些内容，你按照 /development-workflow 写计划吧”批准最终五项范围；以上待批准历史记录已由本次批准取代。

- 校准记录：用户要求 rebase 到 origin/main 后校准并重新审查。基线更新为 `2312cba5`；抽屉、年月、薪资、全屏教育/行业目录及城市名称均已在源码中核对存在，删除“等待另两分支”的执行前置。原五项需求仍未实现，产品契约不变。保留当前共享组件与测试；工作经历样式被公司选择层跨文件引用，不能按单页引用判断删除。旧审查与中途分支状态仅作历史记录，不作为当前执行前置。

- 校准审查 R1：修正 §10 对已合入城市仍要求红绿修复的历史残留，与 §6 统一为按实际改动回归；不改变用户批准产品行为或新增实现范围。
