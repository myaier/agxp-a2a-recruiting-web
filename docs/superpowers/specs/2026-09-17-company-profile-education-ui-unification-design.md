# 公司档案与教育候选展示统一

日期：2026-09-17。状态：用户已认可会话中的修正版范围及主次原则；本文将其落盘，供实施规划使用。尚未实施或运行产品测试。

## 1. 目标与设计权威

Mock Up 是设计源头。共有功能以当前 Mock 的布局、样式、文案和交互为基准，两模式实际调用同一套 JSX／组件和样式。Backend 额外需要的字段、权限、加载、错误和分页状态，尽量复用现有 Mock 设计元素补充。

这不改变 Backend 的真实合同、字段语义、权限、校验、写入时机与媒体生命周期。Mock 不因本轮统一被迫增加 Backend 字段或改变保存规则。存在不同业务规则时，明确由连接层提供参数或回调，不通过组件读取数据源模式暗中决定。

本轮不重新设计视觉。实施前以同一视口、相同展示状态记录当前 Mock；共有部分合并后核对布局、间距、字体、按钮及交互。新增 Backend 状态按同页已有字段／提示风格展示。

## 2. 已确认范围

| 页面／区域 | 当前差异 | 本轮结果 |
| --- | --- | --- |
| 公司档案清单 | 两套清单 JSX；Backend 真实快照与组织门，Mock 本地演示资料 | 从 Mock 清单提取共享展示，注入分区状态及导航；Backend 组织门留外层，只读提示保留 |
| 基本信息名称 | Backend 常用名／品牌名可编辑、核验工商名只读；Mock 一个公司全称 | 沿用 Mock 字段布局，Backend 增加必要名称槽和字段错误；Mock 仍保留公司全称，不新增三名模型 |
| 基本信息其他字段 | LOGO 数据及上传不同；行业目录来源不同；Backend 支持只读 | 共用 LOGO、规模、融资、地址展示，行业保留既有共享正文；权限与回调注入 |
| 公司相册 | 两组各最多 3 张；Backend 即时媒体发布／删除及预览，Mock 压图后草稿保存 | 以 Mock 图格为基准共享图片组，Backend 注入只读与上传预览；保留两端生效时机及文件处理差异 |
| 公司介绍／产品介绍 | 正文已共用，字数同为 500／300；页面编排重复 | 不重写正文；收拢返回／保存栏、标题、分区编排 |
| 主营业务 | Backend 最多 20 条、每条 200 字；Mock 整体 200 字 | 共用现有整屏文本，限制由参数提供；Backend 保留条数和单条校验，Mock 规则不变 |
| 公司福利 | 正文已共用，连接和权限不同 | 保持正文，统一外层编排 |
| 团队介绍 | 正文已共用；Backend 最多 20 人，Mock 不设限 | 保持成员表单，Backend 继续注入上限及只读，共用页面壳 |
| 毕业院校 | Backend 名称＋城市／国家、按 ID 选中、必须有效引用；Mock 名称列表并允许自由文本继续 | 以 Mock 候选行设计为基准共用展示；Backend 保留副标题、请求状态、分页与引用校验 |
| 选专业 | Backend 按 ID 选中、必须有效引用、有请求状态／分页；Mock 本地搜索并允许自由文本 | 同学校；保存、预填、搜索及分页逻辑不重构 |

清单计数继续使用现有计算规则；不把本轮名称槽位共享扩展为完成度算法调整。Backend 品牌名当前使用内部“公司全称”字段承载，不因命名相似将其与常用名或工商名串写。

## 3. 最小组件边界

采用页面局部共享展示：公司清单、公司分区外壳／编排、基本信息共同字段、相册图片组。已有整屏文本、福利、团队、行业选择正文继续使用。仅当前消费者需要的展示参数进入组件，数据获取、草稿、授权、错误处理和写入仍在原连接层。

名称区保留模式各自的字段语义，通过少量显式槽位或局部组件组合复用字段样式；不建立字段 schema、通用表单引擎或通用权限框架。

学校／专业优先复用已有 `src/组件/教育目录候选列表.tsx`。先核对它与当前 Mock 候选行的样式及键盘／选中行为：可直接满足则复用；如果必须改变其它消费者才能适配，则在两页范围内抽取小型候选展示，不扩大底层 `目录候选列表` 的合同。不为复用而牺牲 Mock 设计。

比较方案：仅共用 CSS 不能消除重复 JSX；整体重建统一配置式页面会增加不必要机制；选择上述局部共享，是覆盖现有两种消费者且保留业务差异的最小方案。

## 4. 必须保持的行为

- Backend 只有有效组织关系可查看对应资料；成员只读、管理员编辑资格保持，不用 Mock 默认权限兜底。
- 常用名校验／唯一冲突、品牌名写入、工商名只读保持，缺失核验信息不从其它名称补造。
- Backend LOGO／相册继续使用权威媒体 URL 与原操作；保留预览清理、失败恢复和未发布媒体处理。Mock 继续原压图、本地 LOGO 更新及相册草稿保存行为。
- 保存失败不返回、不提示成功，草稿和错误按现有流程保留；本轮不修改请求协议或时序。
- 行业子页打开时父表单保留草稿但隐藏，返回／焦点及关闭行为保持。
- 教育 Backend 按引用 ID 选中；同名不同 ID 不误选，用户继续输入清除旧引用，点选不导致结果被重新过滤或分页丢失。
- Mock 学校／专业继续允许非空自由文本；不新增目录 ID 必选规则，不伪造城市／国家、请求延迟或服务端能力。

## 5. 并行边界与非目标

源码调查基线为当前分支 `bb6ff6e0`。本次复核两条在办分支：

- `fix/chat-recommend-display@a6d0c8dd`：已改为纯前端范围，仍覆盖聊天身份／头像、简历、推荐解释和企业屏蔽接线；旧跨仓方案不再作为当前依据。
- `baseline-l3-s0-s3-matching@7c0e7479`：测试基线与 STG 双向匹配；可能修改期望职位选择、城市选择、应用水合及共享测试辅助。

本轮产品修改限公司档案清单／分区编辑、学校／专业及必要局部展示组件。主要入口为 `src/屏幕/公司档案编辑.tsx`、`公司档案分区编辑.tsx`、`毕业院校.tsx`、`选专业.tsx` 与对应样式／测试。

不包含已筛候选、屏蔽名单、引导屏蔽、工作经历、聊天、匿名简历、推荐／在谈列表、岗位所属公司区、企业实名认证与企业我的。公司公开页不重做。共享全局状态、数据合同、目录基础设施与共用 E2E fixture 不作为本轮重构对象。

实施前再次检查分支实际及预告改动；低冲突风险不等于承诺无冲突。若提取必须改变上述排除层次，先调整方案保持局部；必要业务变更需单独说明，不能顺带实施。

不新增后端接口、迁移、通用页面配置、状态框架、Mock 网络模拟或跨页面名称模型。只有后续明确产品需求或实际消费者重复需求出现，才重新考虑这些能力。

## 6. 验收责任

1. 结构：共有清单、字段、图片组和候选行实际共用展示组件；不只共用 CSS，也不建立按数据源分出整套 JSX 的伪共享外壳。
2. 视觉：同视口／相同数据状态下，Mock 共有部分相对改动前保持；Backend 共同行遵循 Mock 设计。核对可编辑／只读、名称错误、相册空／满／预览、学校副标题／选中／分页及窄屏溢出。
3. 行为：运行对应四页既有单元测试；按改动补能验证数据映射、权限、保存回调、媒体状态和有效引用边界的测试，不仅测试组件能渲染。
4. 浏览器：依据 `docs/testing/README.md` 选择招聘组织、公司资料及教育选择相关既有用例。实施 Plan 固定具体文件和标题；不修改其它分支正在修的基线断言来取得通过。
5. 静态：typecheck、lint、build、差异空白检查。新增或调整测试项时按现有工具生成清单，集成时协调生成文件，不手改自动区。
6. 本轮设计不包含真实 STG 执行；本地 fixture 不能冒称真实媒体／服务端权限验收通过。出现实际合同或行为变更时重新评估验证范围。

## 7. 批准与后续

用户在看到逐项修正表格后回复“看上去可以”，已确认本文件第 1、2 节方向。本文没有采用此前已撤回的“Mock 对齐 Backend 三名称／必选目录／相册即时保存”等方案。

下一步据本文准备实施计划，明确组件拆分和定向验收选集；产品代码尚未修改。
