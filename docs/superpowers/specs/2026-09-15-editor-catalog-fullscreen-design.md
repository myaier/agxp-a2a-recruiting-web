# 第一批编辑入口目录选择全屏化

日期：2026-09-15。版本：v1.2。

状态：第一批全屏化范围已由用户批准；用户追加学校/专业重复点选缺陷、热门城市名称对齐，并明确批准期望职位页以 Mock 布局为基准拆分展示与数据/状态。本文为修订后的待审阅 Spec，尚未批准进入 Plan。当前会话只交付规划，产品实现交由后续新实施 session。

调查基线：`eeaead9abadfd1e87c4abda19fd3de5a6a66af63`。仓库规则：`CLAUDE.md`、`AGENTS.md`。

## 1. 目标与批准范围

用户已批准先修改四个编辑位置的五个字段入口，将大目录选择从底部抽屉或表单内嵌候选改为全屏：

| 编辑位置 | 字段 | 当前形态 | 目标形态 |
| --- | --- | --- | --- |
| 发布岗位 | 职位类别 | 底部两栏分类抽屉 | 全屏两栏分类选择 |
| 在线简历的教育经历编辑 | 学校 | 输入框下内嵌候选列表 | 字段点击行 → 全屏学校搜索选择 |
| 在线简历的教育经历编辑 | 专业 | 输入框下内嵌候选列表 | 字段点击行 → 全屏专业搜索选择 |
| 在线简历的工作/实习经历编辑 | 所属行业 | 底部树形分类抽屉 | 全屏行业选择 |
| 公司档案基本信息编辑 | 行业 | Backend 内嵌搜索/分类，Mock 固定标签 | 字段点击行 → 全屏行业选择 |

在线简历编辑实现在 `src/屏幕/工作经历.tsx`，同时服务 onboarding 与“我的简历”的跳转入口；不得另建 onboarding 专用的教育/经历编辑实现。公司档案的行业在范围内，公司/企业对象选择不在范围内。

本次统一五个入口的容器与进入/退出方式，另处理用户追加的三项问题：现有 onboarding 学校/专业选择闪烁及再次点选候选消失；热门城市静态名称与目录名称不一致；期望职位页 Backend 未按 Mock 的二级小标题/三级职位布局呈现。成功标准是五个字段均能在完整应用内容区域选择，关闭后回到原编辑上下文，已有合法值与提交边界不受损；既有学校/专业全屏页可稳定重复选择，热门与目录同一城市名称一致；期望职位两模式使用同一布局和样式，Backend 自动提供二级分组与三级职位。

## 2. 方案与最小化取舍

比较三个方案：

1. 只把现有抽屉高度拉满：无法覆盖学校/专业的内嵌输入，也没有统一返回栏与父表单隔离，不满足全部范围。
2. **采用：共用轻量全屏外壳，嵌入各字段已有目录正文及查询适配。** 学校/专业改为独立选择子视图；职业和行业沿用已有可用目录逻辑。追加范围处理学校/专业状态流、城市静态名称、期望职位页展示与数据/状态分离，不扩展为全站重写。
3. 全站统一查询、目录树、单多选事务与全部选择器：会扩大到所有城市/教育/企业等目录的通用抽象，当前不需要；期望职位仅处理用户明确批准的页面边界。

五入口全屏化允许新增的共享能力为一份轻量全屏选择外壳，服务本次五个真实消费者：标题、返回、可滚动内容、必要的焦点边界。业务目录、选择值、API、路由和持久化不进入外壳。期望职位另允许从现有页面提取一份纯展示正文，供 Mock/Backend 两种适配共用，理由和边界见 §5.7。精确文件名及接口在获批后的 Plan 冻结。

继续复用 `教育目录候选列表` / `目录候选列表`、`岗位职业分类正文` 的分类正文、`行业分类列表` 及现有行业查询钩子。不以“统一”为由强迫公司行业迁移到另一套目录深度、模拟选项或查询协议。公司行业的既有内容可适配为该入口两模式共用的展示正文；不要求与简历行业共享所有内部实现。

## 3. 与已有合同的关系

本文获批后，仅在本次五个入口及追加的明确范围内覆盖历史冲突约束，包括：

- `docs/superpowers/specs/2026-09-14-picker-interaction-consistency-design.md` 的简历行业 72% 高度与保持底部外壳约定。
- `docs/superpowers/specs/2026-09-12-core-editors-shared-ui-and-wiring-design.md` 中岗位职业分类保持底部抽屉、教育学校/专业保持表单内候选的约定。
- `docs/superpowers/specs/2026-09-14-recruiter-and-city-fixes-design.md` §6.2 允许精选城市使用中文别名的约定：本次改为同 ID 使用权威目录显示名，原精选 ID、顺序、数量和地域范围保持。

期望职位旧代码/测试中“Backend 右栏无分组、点击父节点下钻并替换右栏”的约束由 §5.7 覆盖。既有 Mock 的分组标题与三级职位网格布局成为两模式共同基准。

旧合同中的目录稳定 ID、Mock 演示数据、岗位不可变字段、行业折叠/分页、业务保存及权限边界仍适用。已有期望行业页面的多选和返回语义不因本次改变。

## 4. 全屏布局与生命周期

### 4.1 进入与视觉

- 五个入口都是字段点击行，显示当前值或明确占位，右侧进入箭头。
- 打开后占满应用内容区域；桌面若有设备外框，以设备内容区域为边界，不覆盖整个桌面窗口。
- 顶部返回按钮与字段标题固定可达。标题分别为“职位类别”“选择学校”“选择专业”“所属行业”“选择行业”。
- 无半屏抓手，不露出下方可操作的原表单；长候选在选择正文内滚动。
- 分类两栏各自滚动可保留；普通候选列表采用单一列表滚动区，避免整页和列表同时滚动。
- 学校/专业保留搜索输入；公司行业保留搜索能力；不顺带为岗位职位类别或简历行业新增搜索功能。
- 短屏、横屏及键盘弹起后，返回与当前结果均可达。打开时先聚焦返回或标题，不自动强制弹出键盘；用户点搜索框后正常输入。

### 4.2 同页子视图

采用当前编辑页面中的本地全屏子视图，不新增路由、隐藏 history 条目、全局草稿存储或 URL 参数。参考岗位城市现有同页子视图方式。

父编辑实例及其草稿保持挂载。选择期间父表单隐藏且不可交互，不进入键盘 Tab 顺序或读屏可访问区域。不能靠纯视觉覆盖冒充隔离。

返回按钮与 Escape 关闭本次选择界面，恢复原字段焦点和父表单滚动位置。浏览器后退沿当前页面已有路由行为离开；不承诺把系统后退改造成子视图关闭。刷新沿已有页面恢复能力，不新增保存保证。

打开、搜索、展开、翻页及关闭不得触发业务保存。关闭后请求响应不能修改已关闭的选择或另一个条目的草稿；重新打开以当前父草稿初始化。

### 4.3 选定与退出

本次五个入口均为单选，采用**点中合法候选即回填父草稿并关闭**。不新增“确定”步骤；这是本次最小方案对先前讨论中“全屏点选后再确定”备选建议的明确取舍。

未点候选时返回，保留父草稿原值。选定仅回填字段，不提交岗位、简历或企业档案；整个表单仍通过原“完成/保存/发布”流程提交。

学校/专业的搜索词改为本次选择局部状态：输入不会先清除父草稿的已有名称或目录引用。当前旧文本可用作搜索初词，但不能按名称自动生成 ID 或自动选择第一条结果。

Backend 的选定原子回填显示名与真实目录引用；同名不同 ID 按 ID 区分。Mock 仍使用本地模拟数据与既有文本/模拟键语义，不产生真实写入。

## 5. 各入口具体边界

### 5.1 岗位职位类别

修改 `src/组件/岗位职业分类正文.tsx` 的承载外壳及 `src/屏幕/发布岗位.tsx` 的接线。保留左右两栏、父节点导航、可选候选点选、目录分页与版本/迟到响应守卫。

保留当前选择语义：左栏负责导航；右栏可选项点选，不可选但有子项时继续展开。不借此改成求职侧的多选网格或重写分类树。

选定继续调用现有类别回填路径，包括类别与岗位名称的既有联动规则。已发布岗位类别仍不可修改，不允许从新的全屏入口绕过锁定。

打开/关闭必须保留岗位当前步骤、名称、JD、薪资、企业坐标及导入运行态。导入的迟到结果不得因选择界面挂载/卸载重复启动或向另一个岗位写入；用户点选通过现有用户修改标志处理。

### 5.2 简历学校与专业

修改 `src/屏幕/工作经历.tsx` 内教育编辑视图。父表单不再直接编辑学校/专业文本，而是点击行打开对应全屏选择。

继续使用 `教育目录候选列表`，学校保留地理副信息，专业保留名称列表。沿用当前搜索防抖、分页、真实目录引用及保存校验；查询只随正在打开的对应字段搜索，不让学校搜索词驱动专业查询。

Mock 新选择也必须点候选；原有 Mock 演示值及历史已填文本保持，不因改成选择行清空。Backend 历史无引用文本继续提示用户选择目录，不自动修补引用。

学位、起止年月、另一字段与教育条目身份始终保留；关闭不退出教育编辑，切换学校不会清掉已选专业，反之亦然。学校或专业成功选定后，仍由教育表单“完成”及上层既有保存路径处理。

onboarding 独立的 `毕业院校.tsx`、`选专业.tsx` 本来已全屏，布局和下一步流程保持；本轮按 §5.5 修复候选状态，不额外统一整个 onboarding 选择器或改变其 Mock 自填规则。

### 5.3 简历所属行业

将 `src/组件/简历行业选择正文.tsx` 改为全屏承载，保留 `行业分类列表` 和 `use行业目录`。

保持单选、可选/可展开分离、多根展开/收起、缓存、分页、重试、目录换版和现有深度边界。开关选择界面不丢失公司、职位、年月、工作内容、项目、实习标记及隐私开关。

### 5.4 公司档案行业

修改 `src/屏幕/公司档案分区编辑.tsx` 基本信息分区，把行业改成进入选择的字段行。Backend 与 Mock 均进入同一全屏外壳和该入口共用的展示正文。

Backend 保留真实 industry 查询、搜索和按节点属性导航；Mock 保留现有 `行业池` 可选值，将其适配为可选节点。允许目录内容和层级由数据决定，不能保留两种不同的容器交互，也不能为了凑层级伪造新行业 ID。

进入、搜索、选定、取消规则相同；不添加 Mock 独占自由输入。已有值照实回显，不能仅因不在当前查询页而清空。搜索中、空结果、错误及加载更多通过共用展示状态表达；失败不能显示成成功空列表。

Backend 只读/无编辑权限时不能打开可编辑选择器。选中只修改公司档案本地草稿；权限、保存校验、revision 与企业媒体处理沿原逻辑。公司名、品牌、LOGO、规模、融资阶段、地址和其余分区保持原有行为。

### 5.5 现有学校/专业全屏页的闪烁与候选消失

用户复现路径：搜索 → 点候选 → 屏幕闪一下；候选再次出现后，再点同一候选 → 内容消失。

定位证据：`毕业院校.tsx` 与 `选专业.tsx` 的 Backend `选候选` 同时回填完整名称并执行 `设候选项([])`；搜索 effect 又依赖该名称。第一次从搜索短词变为完整名称时，候选先清空，再进入 250ms 防抖与重新查询，产生列表消失/重现。第二次点击同项时名称相同，不再触发 effect，但候选仍被清空，搜索状态还保持 success，页面错误地显示“没有匹配结果”。

定向诊断在真实 React 页面组件上注入目录 fixture，分别验证学校和专业：首选后列表为空并进入 loading，第二次查询后结果恢复；再次点同一项后列表为空、出现错误的无结果提示，等待超过防抖时间也没有第三次请求。两项诊断通过，证明当前错误状态流；不是修复通过，也不是浏览器视觉验收。临时诊断文件运行后删除。首次学校诊断 fixture 缺 location 的错误已修正后重跑，不计为产品缺陷。

Mock 专业还存在独立收起分支：`已点选` 将候选隐藏，且过滤掉与完整输入相同的名称，需一并消除“已选项无法在候选中重选”的差异。学校选中标记当前按名称判断，同名不同 ID 必须改为真实引用 ID 判断。

修复合同：

- 搜索查询词与选定答案分别管理；点候选可继续把输入展示为完整名称，但不得把程序回填当成用户重新搜索。
- 点选保留当前成功结果列表及其查询词/分页上下文，显示正确已选状态；重复点同一项保持稳定，不清空、不重查、不误显示无结果，也不丢滚动位置。
- 用户真实修改输入时，才切换搜索词并作废旧引用、游标与在飞请求；下一步继续依赖真实引用有效性。清空、搜索失败、无结果仍各自如实表达。
- 加载更多必须使用该列表对应的原搜索词，不能使用已回填的完整名称混合旧游标。新输入后旧页不可追加；点选不会触发整页重挂载。
- 学校和专业两页均覆盖 Mock/Backend；Mock 保留现有自由文本可继续规则与预填，但不因点选主动隐藏所有候选。
- 保留建档草稿写入、预填 eligibility、保存成功后确认分区及下一步跳转顺序；不把 onboarding 改为点选后直接离页。
- 新增简历学校/专业子视图仍按 §4.3 点选关闭，重新打开必须能看到已选项并再次搜索/重选，不复制上述清空与查询耦合。

### 5.6 热门城市名称与权威目录对齐

根因：`src/数据/城市精选.ts` 固化 `北京`、`上海` 等展示别名；四个入口直接使用它，分省/搜索目录则使用接口的 `display_name`。同 ID 会显示两套名字，已选标签还可能随选择来源变化。

- 集中核对并更新 12 个国内与 10 个海外精选项的显示名。按已有 canonical ID 对照真实目录，不按字符串普遍追加“市”，不修改 ID、国家码、精选数量、顺序或港澳台排除范围。
- 用户指出的北京、上海应显示为“北京市”“上海市”；其他项以同 ID 的目录 `display_name` 为准。海外也按权威名称核对，不根据中文别名猜译名。Plan 阶段需完成名称表核对并记录数据版本/来源；无法核对的项目明确列出缺口，不声称已对齐。
- 当前源码注释引用的 `dogfood-output/2026-09-14-city-ids/` 证据目录在此工作区不存在，因此既有 ID 测试不作为本次名称已核验的证据；使用可用的后端目录快照或只读真实目录响应确认名称。
- 一份配置覆盖 `选工作城市.tsx`（onboarding / 意向）、`选择城市.tsx`（备选城市）、`引导问答.tsx`（城市补问题）、`发布岗位.tsx`（岗位城市）；不逐页维护别名副本，不改全屏结构和选择上限。
- Backend 继续按 ID 关联热门、分组、搜索、已选；同城市不能重复计数。已有引用的旧显示名在选择界面回显时可由对应静态 ID 规范名称，不主动持久化或批量重写历史意向/岗位。
- Mock 当前以城市名为选择键，必须同步适配这 22 个已知精选项在默认列表、搜索、当前/历史、已选及已有草稿中的名称/键，保留演示预填并使简称与正式名称对应同一个选项。采用本次有限映射，不做全站任意地名归一化，也不遍历重写历史存储。
- 不在产品首载时新增 22 次目录请求，不新增后端精选 API、名称同步服务或运行时全目录下载。权威核对只发生于开发/验收。

### 5.7 期望职位：复用 Mock 布局，分离展示与数据/状态

用户给出的截图及明确要求作为本页面视觉与层级合同。范围为 `src/屏幕/选期望职位.tsx` 服务的 onboarding `/onboard/job` 及意向来源 `?来源=意向`，不顺带重做 `引导问答.tsx` 的职位补问题或招聘岗位分类正文。

#### 当前证据

当前虽然只有一份 JSX，Mock 使用 `职业分类树` 的“分组名 + 岗位数组”，Backend 却把当前子项映射为 `组名: ''` 的按钮组；有 `has_children` 的项点击后调用下钻，把 `当前根` 替换为二级节点。三级数据只有二次点击才请求，且一级高亮因此丢失。

规划中已在原页面组件上完成两项临时诊断：三级目录 fixture 的 Backend 必须点击二级“产品经理”才出现“AI产品经理”，随后“产品”高亮消失；Mock 点击“产品”即出现二级标题及三级按钮。两项诊断通过，证实前端适配/状态问题；临时文件已删除。尚未用真实后端目录证明全部类别层级，不能以 fixture 替代数据核查。

#### 共同展示合同

- 左栏只显示一级分类，例如“互联网/AI”“产品”“设计”；点击切换当前一级，当前高亮始终绑定一级 ID。
- 右栏按二级分组排列。二级“产品经理”“游戏策划”“高端产品职位”等作为小标题，不是按钮、不参与选择，也不要求点击展开。
- 各标题下直接显示该分组的三级具体职位按钮，例如“产品经理”标题下显示“产品经理”“AI产品经理”“数据产品经理”等；同名标题与职位可同时存在，身份按不同 ID 表达。
- 保持 Mock 现有标题、搜索框、左右两栏、右侧分组间距和双列网格、底部已选标签与保存按钮的 JSX/CSS 结构与视觉。必要的加载/失败/重试状态使用共同展示，不新增 Backend 专属外观。
- onboarding 保持最多 10 项；意向保持单选。点具体职位只切换本页临时选择，不关闭页面；仍通过底部“保存”回填既有目标。不得套用 §4.3 五个新子视图的点选即关闭语义。
- 搜索保持现有搜索结果视图与全球职位目录查询范围；选择、删除、计数、空选择禁用规则两模式一致。分组型搜索命中只作非选中分组标题，由外层提供对应职位结果，不能再次变成点击后替换浏览右栏的导航按钮。

#### 展示与状态分离

从现有页面提取期望职位纯展示正文，复用现有 `选期望职位.module.css`。它只接收展示值、稳定键、当前状态和用户动作回调：一级分类列表及当前键、右栏二级分组及每组职位、搜索词与搜索结果、加载/错误/分页状态、已选标签、数量上限、返回/保存及选择动作。

正文不得读取 `use应用状态`、路由、Mock/Backend 模式、BFF DTO 或请求 API；不持有目录请求、游标、已选业务引用、全局草稿和持久化状态。不以“Backend ? 一套 JSX : Mock 另一套 JSX”实现；同一展示输入必须渲染出同一结构和操作结果。

外层页面及必要的页面局部钩子拥有数据和状态：

- Mock：把既有 `职业分类树` 适配为一级/二级/三级展示结构，保留现有选项及预填；模拟键在各层稳定、可区分，不发给后端。
- Backend：用真实 ID/parent_id/节点属性查询并构造同一结构，维护一级选择、各分组分页、搜索状态和已选引用。不能把 Mock 职位名或顺序当作真实目录补全数据，也不能按显示名建立父子关系。
- 业务保存仍由外层映射到既有引导预填/意向草稿；展示组件不知道写哪个字段。
- 允许抽取页面局部的目录状态钩子以容纳多分组请求；不创建通用树引擎、全站数据层或额外状态管理库。

#### Backend 加载与边界

选择一级后读取其二级分组，并自动加载各已取得二级的三级职位；不再要求用户点二级。仅加载当前一级所需内容，不预下载全目录。请求组织与并发上界在 Plan 根据真实当前一级分组规模冻结，不暴露新的产品配置。

每个二级分组独立持有子项、游标、加载和错误状态；已有成功分组不因另一组失败被清空。分页继续从右栏滚动触发，按当前一级的稳定分组顺序推进二级页及三级页，保留每组归属，不用一个“当前子项游标”混装所有分组。失败应在对应位置可重试，不静默丢掉整组；明确空组与加载失败。

切换一级后，旧分组及其迟到响应不得混入新一级；搜索与浏览状态分离，清空搜索恢复当前一级分组。保留同查询版本核对和强制刷新机制，换版不得将旧子项拼到新分组。选择和取消只按真实可选职位 ID，已选项不因当前不可见、分页或重试丢失。

Plan 前核实真实 `job-categories` 的一级、二级、三级结构、分页以及 `selectable/has_children`。若存在与该布局冲突的可选父节点、直接可选二级或更深层级，必须就实际证据明确适配决策，不把中间节点冒充最终职位、不静默丢掉合法岗位，也不在实现时临时改回逐层点击。现有 `parentId` 查询能力可支持逐组取数；当前未证明需要后端改动，不预先增加后端任务。若真实数据无法通过现有接口形成目标结构，再报告具体缺口并修订合同。

## 6. 数据与非目标

不新增或修改后端接口、schema、目录 ID、权限、保存时机和草稿持久化。Mock 默认演示数据保留；Backend 失败不回退 Mock。

本次明确不改：公司选择、城市选择的容器/目录请求机制、期望行业多选、向导题序、学历按钮、薪资/日期/数值滚轮、招聘类型、公司规模/融资选项、招聘岗位职业分类的新增搜索功能、全站按钮或目录查询架构。期望职位的现有搜索仅做 §5.7 所需的分组结果适配。

暂缓独立 onboarding 学校/专业整页组件复用（本次状态缺陷修复除外）、岗位/求职职业分类共用正文、公司/简历行业查询机制合并。只有用户另行批准内部组件统一，或有重复修复/行为分叉的实际证据，才评估这些事项。本次不预建全站选择器配置、无限树、虚拟列表或新路由基础设施。

## 7. 验证与完成标准

后续 Plan 应冻结最小定向 Vitest、类型检查及浏览器验证命令，选择依据为实际修改文件与消费者；规划仅为定位追加缺陷运行临时定向诊断，不执行全量产品测试。复用当前 Vitest / Playwright，不新建测试平台。

| 验证边界 | 必须证明 |
| --- | --- |
| 五入口 × 两模式 | 均为全屏，标题/返回存在，学校专业不再内嵌，行业/职位不再半屏 |
| 草稿 | 搜索后取消零回填；点选原子回填并关闭；父表单其余内容、步骤、编辑条目和滚动位置保持 |
| 模式与权限 | Mock 预填保留且零真实写入；Backend 使用真实 ID；已发布类别锁定；公司只读不可进入编辑 |
| 目录边界 | 同名不同 ID；搜索翻页；错误与空结果；关闭/重开/换条目后迟到响应隔离；行业收起和分页不回归 |
| 生命周期 | 父表单保持挂载，岗位导入不重启；教育打开学校不触发专业查询；字段选定不提前保存 |
| 键盘与布局 | 手机常规/短屏/横屏及键盘场景；返回可达；长列表首尾可达；Tab 不进入背景；Escape 关闭并恢复焦点；重渲染不抢焦点 |
| 学校/专业追加缺陷 | 两模式短词搜索、点选、重复点选、换另一项、再次编辑、原查询翻页、迟到响应；选择本身零重查/零空列表闪烁，正确的 ID 选中态与下一步引用守卫 |
| 城市名称 | 22 项按 ID 核对名称；四入口热门/分组/搜索/已选一致；Mock 旧简称预填可回显、同城不重复计数；Backend ID 不变、无额外首载查询 |
| 期望职位两模式布局 | 共同展示正文；一级切换后无需点击二级，二级为小标题、三级为职位按钮；选三级后一级高亮保持；与原 Mock 布局浏览器对照；同输入同 DOM |
| 期望职位状态与数据 | 10 项/单选/保存规则；同名不同 ID；多分组独立分页/失败重试；切大类与搜索的迟到响应隔离、目录换版；真实层级核对，不能用 Mock 词填真实缺口 |
| 未改消费者 | 公司选择抽屉、薪资/日期弹层、现有期望行业多选不因共享组件变更受损 |

浏览器证据必须验证实际布局与交互，不能仅用组件 DOM 存在证明全屏。fixture 测试不得称为真实后端验收；若实际 diff 触及目录/权限/保存接线且现有测试无法证明，按 `docs/dogfood/真实后端行为验收.md` 选择必要真实场景并记录环境及清理责任。

## 8. 后续规划流程

用户审阅并批准本文后，调用 writing-plans 生成一份零上下文 Plan，冻结接口、文件清单、测试责任及宿主执行路由。随后通过 development-workflow 编排 Claude 异构文档 review，并在 Plan 内记录裁决；完成 review 后生成单一双宿主执行 prompt。

本次不生成额外 review/handoff 文档，不启动产品实现，不合入或发布。
