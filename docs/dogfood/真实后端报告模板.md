# 真实后端行为验收报告

按[真实后端行为验收指南](真实后端行为验收.md)选择范围（活动范围只覆盖 STG：
`STG 基础试点` / `STG Onboarding`）。复制本模板到
`dogfood-output/<run-id>/report.md` 后填写；执行开始即写入并持续更新，
保证中断后可恢复。所有业务/节点初始 `NOT_RUN`，做一项填一项。本模板是给人/Agent
填写的 Markdown，没有程序解析合同。

不写入：OTP、Cookie、Authorization、完整认证状态、账号手机号。只读 API 只记
method/path、必要脱敏字段与结论，不记录 Authorization。

> 旧 local 报告段（local fixture 生命周期表、B01–B05 / H01–H04 节点表、七个视觉
> 观察位置、local 等待预算行）已移入历史归档
> [archive/local-report-template.md](archive/local-report-template.md)；历史报告按
> 当时模板与运行时事实保留，不批量改写旧 PASS/FAIL。

## 0. 元信息

- run-id：`<run-id>`
- 执行日期：`<YYYY-MM-DD>`
- 范围：`<STG 基础试点 | STG Onboarding（Case/variant 三元组）>`
- Suite / Case / variant：`<stg-onboarding / stg-onboarding-candidate|stg-onboarding-recruiter / manual|parsed；仅 STG Onboarding 填写，其余填不适用>`
- 环境类型：`<STG>`
- 前端 commit / 工作区状态：`<commit 或 dirty 摘要>`
- 后端工作区与 commit：`<AGXP_MONOREPO_DIR 脱敏描述 + commit>`
- 后端实际部署版本：`<STG receipt 的 deployment tag/source revision>`
- agent-browser 版本：`<--version 输出>`
- 视口 / locale / 时区：`<如 390x844 / zh-CN / Asia/Shanghai>`

## 1. 环境与归属

| 项 | 值 | 归属 |
| --- | --- | --- |
| 前端 dev server | `<URL/PID；实际 VITE_DATA_SOURCE/VITE_BACKEND_ENV>` | `<自起 | 复用（owner 与 env 核对结果）>` |
| 浏览器 URL / API 代理目标 | `<实际 URL / 实际上游；附启动与网络证据>` | `<前端服务>` |
| 候选浏览器会话名 | `<session id>` | 本轮 |
| 招聘浏览器会话名 | `<session id>` | 本轮 |

## 2. 账号与材料来源（不含密钥）

- 候选账号来源：`<STG ephemeral role alias | 专用账号（来源方式）>`
- 招聘账号来源：`<同上>`
- 登录材料获取方式：`<安全来源描述，不含材料本身>`
- STG 登录材料路径：`<.agxp-recruitment-stg-env/sessions/ID/login.json | 不适用>`
- STG receipt 路径：`<.agxp-recruitment-stg-env/receipts/ID.json | 不适用>`
- 合成附件（STG Onboarding）：`docs/dogfood/resources/简历-onboarding.pdf`、
  `JD-onboarding.pdf`（合成基线见 [stg-onboarding](stg-onboarding.md) 第 3 节）

## 3. 等待预算（启动前记录）

| 节点类型 | 预算 | 超时处理 |
| --- | --- | --- |
| 页面加载/水合 | `<如 60s>` | 到期记录最后状态与「原因待定位」 |
| STG prepare/verify/cleanup | `<按现场 skill/CLI 填写>` | 到期先保存回执与状态，再按同 run 恢复/清理 |

## 4. STG 基础试点（两轮）

未选择 `STG 基础试点` 时本节保持 `NOT_RUN`。不得填写手机号、OTP、Cookie、proof
或完整认证状态；身份/号码比较只写受限进程内的结论和允许公开的安全 ID。

### 4.1 operator 生命周期与归属

| 轮次 | STG_RUN_ID | scene | deployment tag / source revision | receipt 路径 | login 路径 | prepare | verify | cleanup | residuals / 占用 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `<id>` | `ephemeral-baseline` | `<实际值>` | `<相对或脱敏路径>` | `<相对或脱敏路径>` | NOT_RUN | NOT_RUN | NOT_RUN | `<未核对>` |
| 2 | `<全新 id>` | `ephemeral-baseline` | `<实际值>` | `<相对或脱敏路径>` | `<相对或脱敏路径>` | NOT_RUN | NOT_RUN | NOT_RUN | `<未核对>` |

- `validate` 结果：第 1 轮 `<状态/证据>`；第 2 轮 `<状态/证据>`
- 前置/占用判读：`<preflight/status 的安全摘要；区分无关 baseline 输入与 ephemeral 必需项>`
- 中断/恢复记录：`<同 run 的继续、cleanup 或 BLOCKED；foreign run 仅记录，不接管>`

### 4.2 第一轮节点

| 节点 | 状态 | 操作后证据 | 刷新/重开证据 | 备注 |
| --- | --- | --- | --- | --- |
| prepare READY、verify 通过；初始资源/归属与实际部署版本已记录 | NOT_RUN | | | |
| 候选 UI 区号+号码+OTP 登录；受限内核对材料；角色/基础资料一致 | NOT_RUN | | | |
| 招聘 UI 区号+号码+OTP 登录；受限内核对材料；角色/基础资料一致 | NOT_RUN | | | |
| 候选普通简历字段修改并刷新回读 | NOT_RUN | | | |
| 候选意向新建 → 编辑 → 删除；每次刷新回读；基准意向保留 | NOT_RUN | | | |
| 招聘名片职务修改并刷新回读 | NOT_RUN | | | |
| 招聘岗位新建 → 编辑 → 归档 → 重开 → 删除；每次刷新回读；基准岗位保留 | NOT_RUN | | | |
| 候选退出生效；招聘会话刷新仍有效且归属正确 | NOT_RUN | | | |
| 自动请求副作用已观察并记录 | NOT_RUN | | | `<method/path + 安全 ID，或确认无额外资源>` |
| 两浏览器会话关闭；cleanup=CLEANED、residuals=[]、占用释放 | NOT_RUN | | | |

**第一轮结论：NOT_RUN**
**第一轮 cleanup：`<NOT_RUN | CLEANED | CLEANED_WITH_RESIDUAL | CLEANUP_BLOCKED>`**

### 4.3 第二轮节点（全新 run）

| 节点 | 状态 | 操作后证据 | 刷新/重开证据 | 备注 |
| --- | --- | --- | --- | --- |
| prepare READY、verify 通过；初始资源/归属与实际部署版本已记录 | NOT_RUN | | | |
| 候选与招聘分别通过独立 UI 会话登录，角色/基础资料一致 | NOT_RUN | | | |
| 意向新建 → 刷新回读 → 删除 → 再刷新 | NOT_RUN | | | |
| 两轮 candidate/recruiter identity 均不同（只记安全 ID/结论） | NOT_RUN | | | |
| 第二轮未继承首轮新建资源 | NOT_RUN | | | |
| 首轮 cleanup 后旧登录材料不能再建立会话（有界正常认证 API 反证） | NOT_RUN | | | `不冒称 UI 登录覆盖` |
| 自动请求副作用已观察并记录 | NOT_RUN | | | `<method/path + 安全 ID，或确认无额外资源>` |
| 两浏览器会话关闭；cleanup=CLEANED、residuals=[]、占用释放 | NOT_RUN | | | |

**第二轮结论：NOT_RUN**
**第二轮 cleanup：`<NOT_RUN | CLEANED | CLEANED_WITH_RESIDUAL | CLEANUP_BLOCKED>`**

### 4.4 试点结论

- 代码回归结论：`<PASS | FAIL | BLOCKED | NOT_RUN>`
- STG 基础试点结论：`<PASS | FAIL | BLOCKED | NOT_RUN>`
- 登录号码核对：`<两角色 UI 输入与各自 login.json 一致；只写结论，不写号码>`
- 身份轮换：candidate `<状态 + 安全 ID/结论>`；recruiter `<状态 + 安全 ID/结论>`
- 首轮旧登录失效：`<状态 + 脱敏证据>`
- 两轮资源不继承：`<状态 + 脱敏证据>`
- 两轮清理：`<各自最终状态、零残留与占用释放结论；失败时写同 run 恢复责任>`
- 排除项：附件/PDF、披露、首次 onboarding、发现/委托、规则、Hosted、MatchCase、IM、
  实名、导出、反馈、公司资料编辑均为 `NOT_RUN`；不得填入 B/H PASS。

## 5. STG Onboarding Suite（stg-onboarding）

未选择该 Suite 时本节保持 `NOT_RUN`。四个选择项（Case × variant）互相独立，每项 =
新 run + 新浏览器会话，不复用上一项资料。步骤、合成基线与判定见
[stg-onboarding](stg-onboarding.md)；业务 / 收尾 / 隔离三个结论分开填写。
不写手机号、OTP、Cookie、proof；只记安全实体 ID 与结论。

### 5.1 选择与版本

- 本次选择：`<Case/variant 三元组列表；支持单个三元组>`
- candidate `manual` `<状态>` / `parsed` `<状态 + 未运行原因>`；
  recruiter `manual` `<状态>` / `parsed` `<状态 + 未运行原因>`
- 前端源码 commit / 实际部署版本：`<commit / receipt 的 deployment tag>`
- 后端源码 commit / 实际部署版本：`<commit / receipt 的 source revision>`
- 生命周期命令核对结果：`<与后端 checkout 核对的实际命令；未实现项写明，对应项保持 NOT_RUN>`

### 5.2 每项生命周期与归属（每项一行）

| 选择项 | STG_RUN_ID | 浏览器会话 | prepare(ephemeral-empty) | verify initial | verify journey | cleanup | deployment tag / source revision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| candidate/manual | | | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | |
| candidate/parsed | | | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | |
| recruiter/manual | | | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | |
| recruiter/parsed | | | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | |

### 5.3 candidate/manual 节点

| 节点 | 状态 | 操作后证据 | 刷新/重开证据 | 备注 |
| --- | --- | --- | --- | --- |
| UI 登录 → 选「我要找工作」进入候选引导 | NOT_RUN | | | |
| 基本资料手填（合成基线） | NOT_RUN | | | |
| 至少一条完整教育（目录点选） | NOT_RUN | | | |
| 求职意向（目录点选） | NOT_RUN | | | |
| 完成落点 → 已完成候选主页 | NOT_RUN | | | |
| 简历/意向刷新持久化 | NOT_RUN | | | |
| 退出重登录 → 直接进主页，不回引导 | NOT_RUN | | | `completed_at` 只读补证可记备注 |

**candidate/manual 结论 — 业务：NOT_RUN / 收尾：NOT_RUN / 隔离：NOT_RUN**

### 5.4 candidate/parsed 节点

| 节点 | 状态 | 操作后证据 | 刷新/重开证据 | 备注 |
| --- | --- | --- | --- | --- |
| 上传 简历-onboarding.pdf + 明确同意处理 | NOT_RUN | | | |
| 真实解析任务成功（有界预算内终态） | NOT_RUN | | | 失败/超时 = FAIL，记录最终状态；不得转手填 PASS |
| 预填与合成基线核对 | NOT_RUN | | | 无证据字段不判对；缺失/错误如实记录 |
| UI 确认/补全后保存并刷新一致 | NOT_RUN | | | |
| 完成 + 持久化 + 回访（同 5.3 末三行） | NOT_RUN | | | |

**candidate/parsed 结论 — 业务：NOT_RUN / 收尾：NOT_RUN / 隔离：NOT_RUN**
未运行原因：`<B02 附件验收未开放 | 真实解析不可用 | 未选择 | …>`

### 5.5 recruiter/manual 节点

| 节点 | 状态 | 操作后证据 | 刷新/重开证据 | 备注 |
| --- | --- | --- | --- | --- |
| UI 登录 → 选「我要招人」进入名片创建 | NOT_RUN | | | |
| 名片 + run 唯一公司选择/创建（记安全组织 ID） | NOT_RUN | | | |
| 名片保存成功 → 完成落点 → 首岗页面 | NOT_RUN | | | |
| 手填首岗（目录点选 + 薪资）并发布且在招 | NOT_RUN | | | |
| 名片/岗位刷新持久化 | NOT_RUN | | | |
| 退出重登录 → 直接进招聘主页，岗位在列 | NOT_RUN | | | |

**recruiter/manual 结论 — 业务：NOT_RUN / 收尾：NOT_RUN / 隔离：NOT_RUN**

### 5.6 recruiter/parsed 节点

| 节点 | 状态 | 操作后证据 | 刷新/重开证据 | 备注 |
| --- | --- | --- | --- | --- |
| 名片 + run 唯一公司 → 完成（同 5.5 前三行） | NOT_RUN | | | |
| 上传 JD-onboarding.pdf + 明确同意处理 | NOT_RUN | | | |
| 真实解析建议成功（有界预算内终态） | NOT_RUN | | | 失败/超时 = FAIL；不得手填后报 parsed PASS |
| 建议稿与合成基线核对；公司按 run 唯一确认、薪资人工补充 | NOT_RUN | | | 未解析字段人工补充 |
| UI 确认后发布且在招 | NOT_RUN | | | |
| 刷新持久化 + 回访（同 5.5 末两行） | NOT_RUN | | | |

**recruiter/parsed 结论 — 业务：NOT_RUN / 收尾：NOT_RUN / 隔离：NOT_RUN**
未运行原因：`<真实 JD 任务不可用 | 终态有限保留不支持 | 未选择 | …>`

### 5.7 收尾、保留与隔离

- journey verify 与页面 PASS 分别结论：`<两者各自状态；journey verify 不代替页面 PASS>`
- retained 资源（类别 / 原因 / 隔离依据）：`<逐条记录；无则写无>`
- residuals / pending / unknown：`<按实际记录；不要求为空>`
- 旧凭据受限反证（cleanup 后一次有界正常认证 API）：`<结论 + 安全 ID；未执行写原因>`
- 两轮衔接证据：`<两个独立 run 的衔接核对：未混入上轮资料/引用、占用释放、旧凭据反证；只跑单项时如实写「两轮隔离未验证」>`

## 6. 问题明细

每问题一条；偶发问题保留首次证据，不因无法稳定复现删除记录。

### 问题 `<NNN>`

- 所属 Case/节点：
- 预期：
- 实际：
- 影响：
- 复现步骤：
- 截图/快照：`<相对路径>`
- 视频（可复现的交互/时序问题）：
- 再次复现结果：`<稳定 | 偶发 | 未再复现>`
- 绕行：`<无 | 描述>`

只读核验记录（method/path、脱敏字段、结论）：

| 时间 | Case | method + path | 关键脱敏字段 | 结论 |
| --- | --- | --- | --- | --- |

## 7. 未覆盖项

| Case/节点 | 状态 | 原因 | 后续责任 |
| --- | --- | --- | --- |

## 8. 结论

- 旧 local B01–B05 / H01–H04 的历史结论按当时报告与运行时事实保留（归档模板见
  [archive/local-report-template.md](archive/local-report-template.md)），不批量改写
  旧 PASS/FAIL，也不因文档归档改判为 PASS。
- STG 基础试点结论：`<PASS | FAIL | BLOCKED | NOT_RUN>`；不得据此把 B02/B04 或
  原 `全部` 改为 PASS
- STG Onboarding Suite：candidate `manual` `<状态>` / `parsed` `<状态>`；
  recruiter `manual` `<状态>` / `parsed` `<状态>`；Suite 隔离
  `<已验证（两轮衔接证据）| 未验证（仅单项运行）| NOT_RUN>`；两个手填变体通过不写成
  四项全通过，也不据此把 B02/B04、原 `全部` 或 `STG 基础试点` 改为 PASS
- 清理结论（单列）：`<完成 | 未完成 | 不涉及>`；未完成时附 receipt 位置、对象归属与恢复步骤
- 整轮结论：`<完整通过 | 部分通过（写明范围）| 未通过 | BLOCKED>`；有 FAIL 不报全通过，
  有 BLOCKED/NOT_RUN 不报全覆盖，清理未完成不宣称整轮通过

## 9. 脱敏摘要去向

长期摘要按 `docs/runs/` 既有约定新建 `<YYYY-MM-DD>-agent-browser-dogfood-<主题>.md`；
原始截图/视频与按需保存的 local 证据副本只留在本目录（已 gitignore），不提交；
STG operator 原始 receipt 留在后端 checkout 的受限目录，报告只引用其路径，不复制或移动。