# 真实后端行为验收报告

复制本模板到 `dogfood-output/<run-id>/report.md` 后填写；执行开始即写入并持续更新，
保证中断后可恢复。所有业务/节点初始 `NOT_RUN`，做一项填一项。本模板是给人/Agent
填写的 Markdown，没有程序解析合同。

不写入：OTP、Cookie、Authorization、完整认证状态、账号手机号。只读 API 只记
method/path、必要脱敏字段与结论，不记录 Authorization。

## 0. 元信息

- run-id：`<run-id>`
- 执行日期：`<YYYY-MM-DD>`
- 范围：`<全部 | 基础行为 | Hosted | Case ID 列表>`
- 前端 commit / 工作区状态：`<commit 或 dirty 摘要>`
- 后端工作区与 commit：`<AGXP_MONOREPO_DIR 脱敏描述 + commit>`
- agent-browser 版本：`<--version 输出>`
- 视口 / locale / 时区：`<如 390x844 / zh-CN / Asia/Shanghai>`

## 1. 环境与归属

| 项 | 值 | 归属 |
| --- | --- | --- |
| 后端 acceptance 栈 | `<健康/自起/复用，profile>` | `<自起（记录 PID）| 他人（owner）>` |
| 前端 dev server | `<URL/PID>` | `<自起 | 复用（owner 与 env 核对结果）>` |
| 候选浏览器会话名 | `<session id>` | 本轮 |
| 招聘浏览器会话名 | `<session id>` | 本轮 |

## 2. 账号与材料来源（不含密钥）

- 候选账号来源：`<fixture 收敛 | 专用账号（来源方式）>`
- 招聘账号来源：`<同上>`
- 登录材料获取方式：`<安全来源描述，不含材料本身>`
- 合成附件：`docs/dogfood/resources/简历-v1.pdf`、`简历-v2.pdf`

## 3. 等待预算（启动前记录）

| 节点类型 | 预算 | 超时处理 |
| --- | --- | --- |
| 页面加载/水合 | `<如 60s>` | 到期记录最后状态与「原因待定位」 |
| PDF 解析 | `<填写>` | 同上 |
| Hosted 解释/推进 | `<填写>` | 同上 |

## 4. fixture 生命周期与资源归属（持续更新）

| 轮次 | Case | scene | BROWSER_FIXTURE_RUN_ID | receipt 路径 | converge | verify | cleanup | 已创建对象 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `<例：H01>` | `<happy>` | `<id>` | `<绝对路径>` | `<状态>` | `<状态>` | `<状态>` | `<对象与原值>` |

中断恢复记录：`<服务/回执核对结果、继续或清理的决定>`

## 5. 基础行为节点表（B01–B05）

节点状态先于 Case 结论；每项含操作后与刷新两份证据（相对路径）。

### B01 候选基准加载

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 简历：姓名/摘要与基准一致 | NOT_RUN | | |
| 简历：刷新后成立 | NOT_RUN | | |
| 意向：职位/城市/薪资/数量与基准一致 | NOT_RUN | | |
| 意向：刷新后成立 | NOT_RUN | | |
| 披露：选中档位正确 | NOT_RUN | | |
| 披露：刷新后成立 | NOT_RUN | | |
| 无 Mock / 无对方私有资料混入 | NOT_RUN | | |

**B01 结论：NOT_RUN**

### B02 候选 CRUD

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 改名 → 刷新确认 → 还原 → 刷新确认 | NOT_RUN | | |
| 新建意向 → 刷新确认 | NOT_RUN | | |
| 意向调薪编辑 → 刷新确认 | NOT_RUN | | |
| 意向删除：目标消失、基准仍在 | NOT_RUN | | |
| 披露改档 → 还原（各刷新确认） | NOT_RUN | | |
| v1 上传 + UI 授权 → 回读 → 刷新仍在 | NOT_RUN | | |
| v2 替换 → 槽位/行核验 | NOT_RUN | | |
| 附件删除 → 刷新后消失 | NOT_RUN | | |
| 解析终态（独立记录，不计入 PASS） | NOT_RUN | | |

**B02 结论：NOT_RUN**

### B03 招聘基准加载

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 名片：姓名/职务/公司与基准一致 + 刷新 | NOT_RUN | | |
| 公司介绍与基准一致 + 刷新 | NOT_RUN | | |
| 在招/归档岗位分组正确 + 刷新 | NOT_RUN | | |
| 无候选私有摘要混入 | NOT_RUN | | |

**B03 结论：NOT_RUN**

### B04 招聘 CRUD

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 职务改→刷新→还原→刷新 | NOT_RUN | | |
| 公司介绍改→刷新→还原→刷新 | NOT_RUN | | |
| 发布岗位：创建成功且在招 | NOT_RUN | | |
| 编辑描述/要求：刷新重开后为改后值 | NOT_RUN | | |
| 停止招聘 → 归档（刷新确认） | NOT_RUN | | |
| 重新开放 → 在招（刷新确认） | NOT_RUN | | |
| 删除 → 目标消失、基准岗位保留 | NOT_RUN | | |

**B04 结论：NOT_RUN**

### B05 双会话隔离

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 双会话各自显示授权数据 | NOT_RUN | | |
| 候选退出生效 | NOT_RUN | | |
| 招聘端刷新仍登录且资料正确 | NOT_RUN | | |

**B05 结论：NOT_RUN**

## 6. Hosted 行为节点表（H01–H04）

### H01 happy 闭环 — 第一次运行

- scene / run ID / receipt：见第 4 节第 `<N>` 轮

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 规则解释就绪并确认生效 | NOT_RUN | | |
| 刷新后有效规则数量 +1 且内容对应 | NOT_RUN | | |
| PDF 上传授权并解析成功 | NOT_RUN | | |
| 候选委托产生 Case（深链） | NOT_RUN | | |
| 候选补充事实并推进 | NOT_RUN | | |
| 招聘端读取同一 Case | NOT_RUN | | |
| 招聘初筛成功 | NOT_RUN | | |
| 双方协调与确认到达终态 | NOT_RUN | | |
| 双端深链复读终态成立、过期动作消失 | NOT_RUN | | |

**H01 第一次结论：NOT_RUN**
**本轮 cleanup：`<完成 | 未完成 | 不涉及>`**

### H01 happy 闭环 — 第二次运行（前轮 cleanup 成功后，新 run ID/receipt）

- scene / run ID / receipt：见第 4 节第 `<N>` 轮

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| （同第一次的节点表） | NOT_RUN | | |

**H01 第二次结论：NOT_RUN**
**本轮 cleanup：`<完成 | 未完成 | 不涉及>`**

### H02 委托失败零 Case（scene p4）

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| AI 不可用被准确解释 | NOT_RUN | | |
| 本次零 Case、无假进展入口 | NOT_RUN | | |
| 刷新后事实一致（必要时只读补证） | NOT_RUN | | |

**H02 结论：NOT_RUN**
**本轮 cleanup：`<完成 | 未完成 | 不涉及>`**

### H03 初筛 attention（scene p5）

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| Case 存在但未继续 | NOT_RUN | | |
| 招聘端显示需注意，原因安全 | NOT_RUN | | |
| 双端原因一致；无 Agent retry 伪装 | NOT_RUN | | |
| 刷新后不变假成功；候选深链复读一致 | NOT_RUN | | |

**H03 结论：NOT_RUN**
**本轮 cleanup：`<完成 | 未完成 | 不涉及>`**

### H04 规则解释失败与草稿恢复（scene p6）

| 节点 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 失败说明明确、无确认成功入口 | NOT_RUN | | |
| 刷新后无新增有效规则 | NOT_RUN | | |
| 关闭失败卡恢复原草稿 | NOT_RUN | | |
| 再次刷新仍无新增规则 | NOT_RUN | | |

**H04 结论：NOT_RUN**
**本轮 cleanup：`<完成 | 未完成 | 不涉及>`**

## 7. 视觉观察（七个位置）

记录实际视口；逐处检查可读性、遮挡、溢出、状态/数据一致性。

| # | 位置 | 所属 Case | 状态 | 截图 | 问题 |
| --- | --- | --- | --- | --- | --- |
| 1 | 候选简历加载后 | B01 | NOT_RUN | | |
| 2 | 候选意向加载后 | B01 | NOT_RUN | | |
| 3 | 候选披露加载后 | B01 | NOT_RUN | | |
| 4 | 候选改名并上传附件后 | B02 | NOT_RUN | | |
| 5 | 招聘名片加载后 | B03 | NOT_RUN | | |
| 6 | 公司介绍加载后 | B03 | NOT_RUN | | |
| 7 | 新岗位发布后的列表 | B04 | NOT_RUN | | |

## 8. 问题明细

每问题一条；偶发问题保留首次证据，不因无法稳定复现删除记录。

### 问题 `<NNN>`

- 所属 Case/节点：
- 预期：
- 实际：
- 影响：
- 复现步骤：
- 截图/快照：`<相对路径>`
- 视频（可复现的交互/时序问题）：
- 再次复现结果：`<稳定 | 偶发 | 未再复现>`
- 绕行：`<无 | 描述>`

只读核验记录（method/path、脱敏字段、结论）：

| 时间 | Case | method + path | 关键脱敏字段 | 结论 |
| --- | --- | --- | --- | --- |

## 9. 未覆盖项

| Case/节点 | 状态 | 原因 | 后续责任 |
| --- | --- | --- | --- |

## 10. 结论

- 各 Case 结论：B01 `<状态>` / B02 `<状态>` / B03 `<状态>` / B04 `<状态>` / B05 `<状态>` /
  H01 第一次 `<状态>` / H01 第二次 `<状态>` / H02 `<状态>` / H03 `<状态>` / H04 `<状态>`
- 清理结论（单列）：`<完成 | 未完成 | 不涉及>`；未完成时附 receipt 位置、对象归属与恢复步骤
- 整轮结论：`<完整通过 | 部分通过（写明范围）| 未通过 | BLOCKED>`；有 FAIL 不报全通过，
  有 BLOCKED/NOT_RUN 不报全覆盖，清理未完成不宣称整轮通过

## 11. 脱敏摘要去向

长期摘要写入 `docs/runs/<YYYY-MM-DD>-agent-browser-dogfood-migration.md`；
原始截图/视频/receipt 只留在本目录（已 gitignore），不提交。
