# 在线简历折叠卡 + 简历原件弹层（2026-08-18/19，含 23:51 纠正返工）

对应用户标注两轮：
- 22:44 第一轮：简历预览从弹层改为时间线顶部折叠卡（已实现，见文末「第一轮记录」）。
- 23:51 纠正：**两个东西要分开** —— 顶部折叠卡是「在线简历」（脱敏版，每单都有）；
  阶段节点里的 PDF 附件行点开是「简历原件」弹层（真名、不打码、PDF 观感）。
  硬规则：不加任何自创解释性小文案。

## 最终实现

### 1. `src/组件/简历预览层.tsx`
- `简历纸身({ 原件?, 代号? })` 具名导出，两个变体：
  - **脱敏（默认）**：候选人 {代号}、联系方式只露首尾（138****6021 / s***@***.com）、
    公司「同赛道公司」。去掉了全部解释性文案（隐去注 / 双盲机制注 / 披露偏好括注 / 纸脚）。
    保留「薪资与到岗」两行（落在贵方区间内 ✓ / 45 天内），不带注释。
  - **原件**：真名（读 `状态.基本信息.真名` = 沈亦舟）、真实联系方式
    （`模拟数据.简历联系方式`）、公司实名、**无**薪资节（双盲产物不属于原件）。
- 默认导出改名 `简历原件层({ 文件名, 关闭 })`：PDF 文档观感 ——
  顶栏只有 PDF 徽标 + 文件名 + 关闭键（对照条已删）；正文灰底上一张白纸。

### 2. `src/屏幕/在谈详情.tsx`
- 顶部折叠卡：标题只有「在线简历」三个字 + 简历图标 + 箭头，**每一单都显示**
  （含 M- 新委托单；原「阶段 ≥ 递交简历」门槛已删）。默认收起，点头展开内联 `简历纸身`。
- PDF 附件行点击恢复为**打开弹层**（`简历原件层`），滚动机制整套删除
  （scroll effect / overflowAnchor / 卡片 ref）。
- 切单（同路由换参数）时折叠卡收起、弹层关闭。

### 3. `src/屏幕/在谈详情.module.css`
- 简历卡样式简化：单行折叠头（13.5px/700 标题 + 箭头），白卡无边框 + 极淡投影。

### 4. `src/组件/简历预览层.module.css`
- 删：对照条 / 隐去注 / 薪资注 / 纸脚样式；加：PDF 徽标、纸底（灰）+ 白纸（页）样式。

### 5. `src/数据/模拟数据.ts`
- 新增 `简历联系方式 = { 手机: '138 0217 6021', 邮箱: 'shenyizhou@qq.com' }`
  （原件用；与脱敏版打码首尾一致）。

## 自测记录（dev http://127.0.0.1:8083，mcp__Claude_Browser，均 PASS）

| 场景 | 断言 |
|---|---|
| `#/deal/J-01` 顶部「在线简历」默认收起 → 展开 → 收起 | aria-expanded 翻转 + 截图 |
| 展开内容（脱敏）| 候选人 A-01、138****6021、s***@***.com、「同赛道公司」无括注、薪资两行、无隐去注/双盲注/纸脚 |
| 点 PDF 附件行 → 原件弹层 | 弹层子树内：PDF 徽标 + 沈亦舟_简历_2026.pdf + 沈亦舟 + 138 0217 6021 + shenyizhou@qq.com + 公司实名 + 无薪资节 + 零解释文案；✕ 关闭成功 |
| `#/deal/J-02`（需要协调）| 卡显示、收起 → 展开为候选人 A-02 |
| `#/job/M-01` 委托入谈 → `#/deal/M-01`（匿名初筛）| 「在线简历」卡显示（新要求）、默认收起、可展开 |
| `npm run typecheck` / `npm run build` | 0 error |

## 并发说明

- 同仓另一会话在本轮返工期间提交了 commit（ff1ac57、8f4860a），把本任务改动的文件
  一并收进了提交；本会话自身未执行任何 git commit。
- 途中出现过的 `通用.tsx` TS6133 错误属于该会话的中间态，其后续提交已自行修复。

---

## 第一轮记录（22:44 版，已被 23:51 纠正取代）

第一轮把弹层内容整体搬进顶部折叠卡（标题「递出去的简历 · 对方看到的版本」，
阶段 ≥ 递交简历才显示，附件行点击=展开+平滑滚动）。过程中修掉的通用问题仍然有效：
scroll anchoring 与平滑滚动冲突、Rules of Hooks（effect 不能放提前 return 后）、
切单状态残留。滚动相关机制在第二轮随交互回退一并移除。
