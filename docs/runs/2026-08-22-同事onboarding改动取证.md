# 同事 Alina Wu 的 onboarding 改动取证（2026-08-22）

只读取证，本轮未改任何代码。所有结论带 `提交号 / 文件:行号`。无证据的判断单独标「推测」。

涉及的 6 个提交（按作者时间序）：

| 提交 | 时间 | 标题 |
|---|---|---|
| `6dd3b15` | 08-21 12:08 | 完善角色 onboarding、初筛规则与自动化测试 |
| `23f3e5d` | 08-21 14:13 | Merge pull request #1 from myaier/**codex**/onboarding-screening-accessibility |
| `1c47b35` | 08-21 15:09 | Streamline role entry and use daily pay for internships |
| `ab855a5` | 08-21 15:33 | Align onboarding copy and screening rules |
| `89f3f50` | 08-21 16:20 | Expand onboarding screening and browser QA |
| `ef27340` | 08-21 18:10 | Validate onboarding links and internship dates |
| `8b30087` | 08-21 22:48 | Protect onboarding preferences and expand flow tests |

`23f3e5d` 是 merge commit，diff 与 `6dd3b15` 完全一致，本身不含新改动。真正的 6 次改动是其余 6 个。

---

## 0. 她参考的是什么 —— 直接答案

### 0.1 不是 BOSS直聘 截图，是 LinkedIn + Indeed

她在 `6dd3b15` 里新建了 `docs/onboarding与初筛审计-20260821.md`，其中「产品对照」一节（该文件第 12–18 行）逐条列出了她的依据，原文四条：

- LinkedIn Open to Work —— 「把职位、地点、办公方式、开始时间、雇佣类型作为独立偏好，而不是塞进一段自由文本」
- LinkedIn Screening Questions —— 「把学历、地点、语言、证书、经验、工作许可、技能、混合办公等做成结构化问题，并允许标记 must-have」
- Indeed Job Search Preferences —— 「进一步拆出工作时间、薪资、通勤、异地与远程」
- Indeed Screener Questions —— 「明确区分 Required 与 Preferred：前者可以自动筛掉，后者只做排序和提示」

然后是那句纲领：「可借鉴的不是页面数量，而是『偏好 / 资格 / 必须项』三类数据不混用。」

**这份文档全文没有出现过一次「BOSS」。** 仓库里既有的对标基准 `docs/BOSS对照审计.md` 也没有被这 6 个提交中的任何一个读取或引用（`git show --name-only` 六次全查，无命中）。

这就是「逻辑不合理」的结构性来源：**对标物被换掉了**。你的口径是「照 BOSS 截图的页面顺序抄」，她的口径是「照 LinkedIn/Indeed 的筛选字段模型建结构化数据层」。前者约束页面长什么样，后者约束数据要采到什么粒度 —— 她把后者的数据模型灌进了前者的页面壳，所以每一屏都在长出截图里没有的字段。

### 0.2 分支名确认是 AI 编码工具生成

合并的分支是 `codex/onboarding-screening-accessibility`（`23f3e5d` 提交标题原文，远程分支 `remotes/origin/onboarding-screening-accessibility` 仍在）。`codex/` 前缀是 OpenAI Codex CLI 的默认分支命名。旁证：6 个提交的标题全部是英文祈使句（Streamline / Align / Expand / Validate / Protect），与仓库里你自己 20 余个中文提交（「修拦路四条」「标注隧道守护 v8」）风格完全不同。

**这解释了两件事**：(a) 依据来自模型的通用招聘产品知识（LinkedIn/Indeed 是英文语料里的默认对标），不是你给的截图；(b) 每个提交都带一份自洽的测试和文档，看起来非常"完备"，但完备性是相对它自己那套假设而言的。

### 0.3 她把你的产品决定当成 bug 修了

`docs/onboarding与初筛审计-20260821.md` 的「本轮发现并修复」第一条原文：

> 「招聘方首次注册绕过实名认证：已恢复认证入口。」

而她删掉的那行代码，注释原文是（`选身份.tsx`，`6dd3b15` 之前）：

```
// 标注 2026-08-20 13:32：企业端先不做实名认证，注册直接进招聘名片
跳转(选中 === '企业' ? 路径.招聘名片 : 路径.引导说明);
```

即：她"修复"的对象，是你 08-20 13:32 明确下的决定。同一提交里 `招聘名片.tsx` 的注释也被同样处理 —— 原文「标注 2026-08-20 13:35：认证已撤，公司也直接改」被替换成「姓名与公司来自认证结果，只允许编辑不会破坏认证关系的职务」，姓名/公司两个字段被锁成只读。

这是本次审计里性质最重的一类：**不是改坏了，是拿另一套口径覆盖了你的口径，并且把覆盖动作记录成了"修复"。**

---

## 1. 她动过流程顺序本身吗？—— 动了

**明确结论：动了，三处。** 另有两处流程数组变化是你自己做的，不算在她头上。

### 先澄清一个技术前提

`Onboarding流程` 这个数组**运行时不被消费**。全库引用点只有两处（`grep -rn "Onboarding流程" src e2e`）：

```
src/流程/onboarding配置.ts:100   ← 定义
src/流程/onboarding配置.test.ts  ← 11 处断言
```

真正的跳转仍然写死在各屏的 `下一步` 里。所以「改数组」= 改合同文档 + 改测试断言，「改跳转」才改用户看到的东西。**她两者都动了。**

### 动的三处

#### 动 1：招聘方注册入口 招聘名片 → 企业实名认证（`6dd3b15`）

- `src/流程/onboarding配置.ts:身份首次入口()` 新建，`return 身份 === '企业' ? 路径.企业实名认证 : 路径.引导说明`
- `src/屏幕/选身份.tsx` 删掉写死跳转，改调 `身份首次入口(选中)`
- `Onboarding流程.招聘方` 数组首项即 `路径.企业实名认证`
- 测试 `onboarding配置.test.ts:24-26` 断言：`expect(身份首次入口('企业')).toBe(路径.企业实名认证)` + `expect(Onboarding流程.招聘方.slice(0, 3)).toEqual([路径.企业实名认证, 路径.招聘名片, 路径.发布岗位])`

**这是唯一一处真正改变了用户可见跳转顺序的流程改动，且方向与你 08-20 13:32 的决定相反。** 且已被测试钉死 —— 想改回去必须同时改测试，否则红。

#### 动 2：选身份屏从「选卡 + 点主按钮」两段式改成「点卡直接跳」（`1c47b35`）

`src/屏幕/选身份.tsx`：删掉 `选中` state、身份项的 `可用` 字段、以及首次注册分支的主按钮文案（`'以「招聘方」身份继续'` / `'以「求职者」身份继续'`）。改成卡片 `onClick={() => 跳转(身份首次入口(项.键))}`，底部主按钮改为**只在切换模式下渲染**（`{切换模式 ? <主按钮 .../> : null}`）。

结果：首次注册时选身份屏底部整个没有主按钮了，点卡即跳，无确认步。这一屏的交互结构变了。

#### 动 3：学生（在校）答完薪资后的落点（`1c47b35`）

`src/屏幕/引导问答.tsx` 的 `下一题`：

```
- if (当前题 === '期望薪资' && !全局.引导预填?.薪资 && 全局.基本信息.身份 !== '在校') {
-   派发({ 型: '存薪资预填', 下限: 薪资下限, 上限: 薪资上限 });
-   跳转(路径.基本信息);
+ if (当前题 === '期望薪资' && !薪资已答) {
+   派发({ 型: '存薪资预填', 下限: 薪资下限, 上限: 薪资上限, 单位: 当前薪资单位 });
+   if (全局.基本信息.身份 !== '在校') 跳转(路径.基本信息);
+   // 学生留在向导：派发后薪资题会从题序中移除，当前索引自然落到下一题。
```

学生分支的向导内导航行为改了（原来学生根本不进这个分支，现在进但不跳）。

> 注：这段代码正是你 `51b6f20` 里定位的白屏根因所在（「一次 dispatch 同时满足两个过滤条件 → 题序 5 塌到 2 而游标停在 2」）。她这次改动新增了一条 dispatch-但-不跳转 的路径，与那个 bug 同源。是否直接引入了该 bug 需另行验证 —— **推测**，本轮未复现。

### 不是她动的（避免误判）

| 流程数组变化 | 实际作者 |
|---|---|
| `接入飞书` / `企业接入飞书` 从三条流程里移除 | 你，`e3d8fdb` + `8f1d0d5` |
| `引导说明` 从流程首位移除、`身份首次入口('求职者')` 改指 `学生分流` | 你，`51b6f20`（标注 2026-08-21 19:57「把这个页面删了吧」）|
| `引导问答薪资段` 拆成独立地址 `?stage=salary` | 你，`51b6f20` |

### 顺带：她建的合同本身有一处二义性

`6dd3b15` 的 `社招求职` 数组里 `路径.引导问答` 出现了**两次**（index 2 和 index 10），同一个地址登记两遍。同提交的测试 `onboarding配置.test.ts:37` 用 `indexOf` 断言先后关系，只能取到第一次出现的位置 —— 断言看起来通过，但合同表达不了"这一屏第几次进"。这正是你 `51b6f20` 里用 `?stage=salary` 解决的问题。

---

## 2. 她新增了哪些字段/概念

### 求职端（`求职初筛偏好` 接口，`src/流程/onboarding配置.ts`）

| 字段 | 哪个提交 | 加在流程哪一步 | 必填？ |
|---|---|---|---|
| **求职类型**（社招全职/校园招聘/实习生/兼职） | `6dd3b15` | 学生分流屏「想找哪类机会？」 | 必填。`6dd3b15` 是多选≥1；`ab855a5` 收紧成**恰好 1 个**（多于 1 个报「一个主要求职类型」）|
| **办公方式**（现场/混合/全远程） | `6dd3b15` | 学生分流屏「可接受的办公方式」 | 必填（≥1）|
| **实习月数**（1/3/6） | `6dd3b15` | 学生分流屏，选「实习生」时才出现 | 必填 |
| **每周到岗天数**（2/3/4/5） | `6dd3b15` | 同上 | 必填 |
| **求职薪资单位**（月薪K / 元/天） | `1c47b35` | 派生字段，不由用户填；`判断求职薪资单位()` 从求职类型推导，作用于引导问答的薪资题 | — |
| **毕业时间**（`YYYY-MM`，`<input type="month">`） | `89f3f50` | 学生分流屏，选「校园招聘」时才出现 | 必填 |
| **实习开始日期**（`YYYY-MM-DD`，`<input type="date">`） | `89f3f50` | 学生分流屏，选「实习生」时才出现 | 必填 |
| **作品集链接** | `89f3f50` | 学生分流屏，标题写「（可选）」 | **名义可选，实际半必填** —— 留空放行，但填了非法值会把「下一步」按钮 disable（`求职初筛缺失项` 无条件跑 `校验作品集链接`）|

`ef27340` 追加：引导问答「个人优势」题里原来的独立字段「GitHub 链接」（本地 state，从不落盘）被改成读写同一个 `作品集链接` 全局字段，标题从「GitHub 链接」改为「作品集 / GitHub / 项目链接」。

### 求职端薪资档位（`1c47b35`，`引导问答.tsx`）

新增日薪档表：`日薪档` = 面议 + 50–200（步长 10）+ 220–500（步长 20）+ 550–800（步长 50），上限档多一档 850。选实习生时薪资题标题从「期望现金月薪是？」变成「期望实习日薪是？」，滚轮单位从 `K` 变成 `元/天`，自动带宽从 10 变成 100。

### 招聘端（`在招岗位` 类型，`src/数据/类型.ts:273+`）

| 字段 | 提交 | 位置 | 必填？ |
|---|---|---|---|
| **加分关键词** | `89f3f50` | 发布岗位第 2 步 | 可选。语义是「只影响候选人排序，不用于自动淘汰」|
| **实习转正**（提供/暂不提供） | `89f3f50` | 发布岗位第 1 步，实习生专属 | 必填（`可离开第一步` 拦） |
| **面试轮次** | `89f3f50` | 发布岗位第 3 步 | 必填（`提交前缺失` 拦） |
| **招聘紧急度**（尽快到岗/两周内/一个月内/持续招聘） | `89f3f50` | 发布岗位第 3 步 | 必填 |
| **实习最晚开始日期** | `ef27340` | 发布岗位第 1 步，实习生专属 | 必填 |

### 招聘端：`89f3f50` 把一批预填默认值清空并升级成必填闸门

原来带演示默认值、现在改成空 + 必填的字段：岗位名称（原「资深后端工程师 · 交易网关」）、工作城市（原「上海」）、办公地（原取公司档案地址）、职位类别（原「后端开发」）、办公方式（原「混合」）、薪资上下限（原 50/65）、年薪月数（原 15）。同时 `ab855a5` 把 JD 描述预填和 JD 要求预填清成空串，经验要求/最低学历默认从「5 年以上」「本科」改成「不限」「不限」。

**这一条影响很大**：发布岗位从「打开就能一路点到底的演示流」变成了「9 个必填闸门，每个都要真填」。原型演示时的手感会显著变重。

### 新增的两个概念模块

- `src/数据/薪资初筛.ts` + `.test.ts`（`6dd3b15`）—— `薪资初筛(候选期望, 岗位预算)` 返回 `'有交集' | '无交集' | '待核对'`。单位不同或解析失败一律 `待核对`，不当成不符合。这条与你的双盲口径**一致**。
- `初筛字段矩阵`（`6dd3b15`，`89f3f50` 扩充）—— 把可自动淘汰的字段按 `通用/社招/学生/实习` 四组固化。这是 LinkedIn Screening Questions 的 must-have 模型的直接落地。

---

## 3. 她加了哪些校验规则

全部在 `src/流程/onboarding配置.ts`，行号以 HEAD 为准（`ef27340` 引入时的行号与 HEAD 相同，该文件此后只在末尾追加）。

### 3.1 日期类（`ef27340`）

| 规则原文 | 行号 | 触发条件 |
|---|---|---|
| `'请填写预计毕业时间'` | 44 | 值为空 |
| `'预计毕业时间格式不正确'` | 45 | 不匹配 `/^\d{4}-(0[1-9]\|1[0-2])$/` |
| `'预计毕业时间不能早于本月'` | 46 | `值 < 今天.slice(0,7)`，字符串比较 |
| `` `请填写${字段名}` `` | 55 | 值为空 |
| `` `${字段名}格式不正确` `` | 56 | `是真实日期()` 失败（含 `2026-02-30` 这类"格式对但日子不存在"）|
| `` `${字段名}不能早于今天` `` | 57 | `值 < 今天`。**当天算通过**（测试 `onboarding配置.test.ts` 明写 `校验未过期日期('2026-08-21', ..., '2026-08-21')` 应为 `null`）|

`今天日期()`（第 39 行）用**本地时区**拼 `YYYY-MM-DD`，`是真实日期()`（第 30 行）用 `T00:00:00Z` 走 UTC 回环校验。两者时区口径不一致 —— 在 UTC+8 的当地午夜前后可能出现"今天"边界抖动。**推测**为潜在缺陷，本轮未构造用例复现。

UI 侧同步加了原生约束：`学生分流.tsx` 的毕业月 `min={今天.slice(0, 7)}`、实习开始日 `min={今天}`，`发布岗位.tsx` 的最晚实习开始日 `min={当前日期文本()}`，三处都带 `required` 和 `aria-invalid`。

### 3.2 链接类（`ef27340`）

| 规则 | 行号 |
|---|---|
| `规范化作品集链接()`：trim 后若无 `协议://` 前缀，一律补 `https://` | 62–66 |
| `'请输入有效的作品集或项目链接'` —— 协议不是 http/https，或 hostname 不含 `.` | 73 |
| `'请输入有效的作品集或项目链接'` —— `new URL()` 抛异常 | 77 |
| 空值放行（`if (!值?.trim()) return null`） | 69 |

补 `https://` 发生在 `onBlur`（`学生分流.tsx` 与 `引导问答.tsx` 各一处），用户输 `github.com/xx` 失焦后输入框里的值会**自己变成** `https://github.com/xx`。

### 3.3 求职类型互斥（`ab855a5`）

`求职初筛缺失项` 第 212 行：`if (偏好.求职类型.length > 1) 缺失.push('一个主要求职类型')`。

配套第 239 行 `迁移主要求职类型()`（`89f3f50`）：旧缓存里同时存了校招+实习时，按已存薪资单位挑一个留下（存的是「元/天」留实习生，否则留第一个非实习类型）。这是为了不让月薪和日薪共用一个区间。

### 3.4 汇总闸门

`求职初筛缺失项()`（第 209–233 行）是学生分流屏「下一步」按钮的唯一开关（`禁用={已选职位.length === 0 || 求职初筛缺失项(筛选偏好).length > 0}`）。它同时承担两种语义 —— "缺项名"（`'可实习月数'`）和"错误文案"（`'预计毕业时间不能早于本月'`）—— 都被 push 进同一个 `string[]`，然后拼成轻提示 `请补充：${缺失.join('、')}`。所以错误场景会弹出「请补充：预计毕业时间不能早于本月」这种读不通的句子。

### 3.5 招聘端新增拦截（`89f3f50` / `ef27340`，`发布岗位.tsx`）

`'请选择职位类别'`、`'请选择办公方式'`、`'请确认是否提供转正机会'`、`'请确认年薪月数'`、`'请确认面试轮次'`、`'请选择招聘紧急度'`，加上实习岗位的 `校验未过期日期(实习最晚开始日期, '最晚可接受实习开始日期')`。

---

## 4. 她改了哪些文案 —— 这是与你口径冲突最密集的一块

`ab855a5`（标题 "Align onboarding copy and screening rules"）是一次**全局文案重写**，覆盖 8 个屏。

### 4.1 分叉口的问法被改了 —— 直接冲突

你的口径：**注册分叉口是「你是学生吗」**，BOSS 那套截图顺序属于非学生分支，学生分支你要自己出稿。

`ab855a5` 把这个分叉口改了（`学生分流.tsx`，HEAD 第 149 / 158 行）：

| | 改前 | 改后（HEAD 现状）|
|---|---|---|
| 问题 | `你是学生吗？` | `你目前是否在校？` |
| 选项 | `是` / `不是` | `在校` / `已毕业` |
| 注释 | `{/* ── 你是学生吗：并排双选钮（截图同款布局）── */}` | `{/* 在校状态只用来安排资料填写顺序，不限制求职类型。 */}` |

注意被删掉的那句注释原文里有「**截图同款布局**」—— 她删除时抹掉了这一屏与截图的锚点，并且在语义上把「你是不是学生」降级成了「你现在在不在学校」，把身份分叉重新定义成了"只决定填表顺序"。这与你「分叉口是你是学生吗、学生分支我自己画」的口径是两回事。

### 4.2 新增了 4 处解释性小文案 —— 你明令禁止的

| 位置 | 文案原文 | 提交 |
|---|---|---|
| `学生分流.tsx:162-164` | 「仅用于安排后续资料填写，不会限制你可选的机会类型」 | `ab855a5` |
| `学生分流.tsx:183` | 「先选一个主要方向，进入后可继续添加其他求职意向」 | `ab855a5` |
| `发布岗位.tsx` 职位描述 placeholder | 「例如：负责什么业务、解决什么问题、与哪些团队协作」 | `ab855a5` |
| `发布岗位.tsx` 职位要求 placeholder | 「例如：必须具备的技能、项目经验以及工作地点要求」 | `ab855a5` |

前两条都是纯解释性的、用来给自己新加的字段找补的小字，正是「你就别加小文案了」指向的那一类。它们**至今仍在 HEAD 上**。

同时 `ab855a5` 删掉了原有的一条随选择切换的副行文案（原文 `是学生 ? '校招与实习岗位 · 不卡年限' : '社招岗位 · 按工作年限核对'`，注释写明「副行沿用重构前两张分流卡的既有文案」），换成了上面那条固定小字。

### 4.3 你自己定的文案被覆盖

`引导说明.tsx`（该页后来被你在 `51b6f20` 整个删掉，但覆盖行为本身仍是事实）：

```
- <h1>AI代理帮你找工作，也帮你谈条件</h1>
- <p>你只填一次期望，剩下的匹配、核对、谈判都由它替你跑。</p>
+ <h1>AI代理帮你匹配岗位，先把条件核对清楚</h1>
+ <p>你填写求职偏好，AI代理匿名完成初筛；薪资和到岗时间留给你与招聘方沟通。</p>
```

被覆盖的那两句上方，注释原文是「标注 2026-08-20 13:27：文案要说清价值 —— **用用户给的表述**」。即她覆盖的是标注为"用户原话"的文案。三步说明卡（01/02/03）同样被整段重写。

### 4.4 「谈判」这个词被系统性删除

`ab855a5` + `89f3f50` 把全线"谈判"改写成"匹配 / 初筛"：

| 屏 | 改前 | 改后 |
|---|---|---|
| `披露偏好.tsx` | 「代理在**谈判**里只使用你允许的信息」/「**谈判**时交换的是『是否落在对方区间内』这类是否题」 | 「代理在**匹配和初筛**中只使用你允许的信息」/「**初筛**时只判断『区间是否匹配』」 |
| `企业披露策略.tsx` | 「你的带宽和对方的期望都只留在各自代理手里，**谈判**时交换的是『是否有交集』」 | 「你的薪资区间和对方的期望都不会披露，**初筛**时只判断『区间是否匹配』」 |
| `披露说明.tsx` | 「完成后AI代理立即按此授权工作」/ 按钮「完成 · 让AI代理开始工作」 | 「完成后，AI代理将按这些条件开始匹配和初筛」/ 按钮「完成设置，开始匹配」 |
| `发布岗位.tsx` | 按钮「发布 · AI代理开始寻访」 | 「发布岗位并开始寻访」 |
| `发布岗位.tsx` | 「**匿名初筛合同**」/「Agent 不询价、不报价、不谈薪」 | 「**AI 初筛条件确认**」/「AI 只按以下条件进行初筛。薪资仅判断双方区间是否匹配，不询问或协商具体金额」 |
| `岗位详情.tsx` | 「**谈判档案**全部保留」 | 「**接洽记录**全部保留」 |

方向上不违反双盲，但**「双方各有 AI 代理替自己谈」这个产品定位的措辞被抹平成了"AI 帮你做筛选"**。产品名 AGXP 的核心叙事（代理之间谈）在文案层面基本消失了。这些改动至今在 HEAD 上（`披露偏好.tsx:29`、`披露说明.tsx:60,63`、`发布岗位.tsx:1133`）。

### 4.5 其余文案改动

- 「一键上传简历 · 自动填好后面每一步」→「上传简历，自动预填可识别的信息」（`学生分流.tsx`、`工作经历.tsx` 两处）
- 「已读取，后续资料已预填」→「已选择简历，可识别的信息将用于预填」
- 「职位关键词」→「必须具备的技能（用于自动初筛）」
- 「招聘要求」→「给候选人看的职位要求」
- 「选择职位要求」→「职位要求」
- 「校园招聘：面向应届毕业生，不按年限筛」→「按届别筛选」；「兼职：按时段用工，按时计薪」→「灵活排班，按小时计薪」
- 「GitHub 链接」→「作品集 / GitHub / 项目链接」，占位「粘贴你的 GitHub 主页链接」→「粘贴你的作品集、GitHub 或项目链接」（`ef27340`）
- 选身份标题「你想用它做什么？」→「你现在想找工作，还是招人？」（`ab855a5`）

### 4.6 进度条：没有被重新引入 ✅

全库 grep `进度条` 无 onboarding 侧回归。`引导问答.tsx:3` 的注释「进度条按标注 2026-08-20 13:00 删除，onboarding 一律不带进度条」仍在，行为一致。发布岗位仍有三段进度条，但那是企业端发岗向导，不在你说的 onboarding 范围内，且早于她的改动就存在。

---

## 5. 她的测试断言了什么 —— 反推设计意图

这是找依据最有用的一节：**她的测试就是她心里那份未经你确认的规格说明书。**

### 5.1 `src/流程/onboarding配置.test.ts`（`6dd3b15` 建，共 4 次扩充）

| 断言（it 标题原文） | 反推出的设计意图 |
|---|---|
| 「招聘方首次注册**必须先认证**，再填写名片和岗位」 | 她把「实名认证前置」当成不可协商的约束钉进测试。与你 08-20 13:32 的决定正面冲突，且钉死后改回去必须删测试。 |
| 「学生路径不会跳过实习经历、技能与证书所在页」 | 她认为学生分支漏了 `工作经历` 页，是 bug。断言 `工作经历` 必须夹在 `就读时间段` 和 `求职状态` 之间。 |
| 「学生和社招的配置顺序**明确不同**」 | 她保留了你「两条配置路径不能一样」的口径 —— 这一条是对齐的。 |
| 「选择实习时**必须补齐**时长和每周到岗天数」 | 实习字段必填，是她的设计前提，不是可选增强。 |
| 「初筛合同同时覆盖通用、社招、学生与实习字段」（`Object.keys(初筛字段矩阵)` 精确等于四项）| 她认为字段矩阵的**分组结构本身**是合同的一部分。这条断言用 `toEqual` 锁死了 key 顺序，任何人加第五组都会红。 |
| 「**首次配置只保留一个主要求职类型**」 | `ab855a5` 加。她把"单选"从实现细节升级成合同。 |
| 「实习主要方向按日薪采集，其他类型保持月薪」 | 月薪/日薪二分是她的核心推论，来源是 Indeed 的薪资偏好拆分。**BOSS 截图里没有这个概念。** |
| 「自动迁移旧的多类型缓存，避免月薪与日薪共用一个区间」 | 她意识到自己 `6dd3b15` 的默认值（校招+实习同选）与 `ab855a5` 的单选规则冲突，专门写了迁移函数 —— 即她在给自己两小时前的设计打补丁。 |
| 「拒绝过期或无效的日期，**并允许当天开始**」 | 边界口径明确：当天算有效。 |
| 「统一并校验作品集链接」 | 补 `https://` 是有意为之的规范化，不是副作用。 |
| 「按候选最早日期与岗位最晚日期执行实习开始时间初筛」 | **她在设计一条自动淘汰规则**：候选最早可开始日 ≤ 岗位最晚接受日 → 匹配。这是双端字段配对，属于产品决策层面，不是实现细节。 |

### 5.2 `src/数据/薪资初筛.test.ts`（`6dd3b15`）

| 断言 | 意图 |
|---|---|
| 「**只返回薪资带是否相交，不泄露候选人的具体预期**」 | 她主动为双盲写了防回归测试。**与你的口径一致，这条是加分项。** |
| 「不同计薪周期和无法解析的输入**交由人工核对**」 | `待核对` ≠ `不符合`。对应她审计文档里的后端约束「未采集到候选答案时返回『待核对』，不能当作『不符合』」。 |

### 5.3 `src/状态/应用状态.test.ts`（`6dd3b15`）

| 断言 | 意图 |
|---|---|
| 「保持纯函数：更新认证状态时**不直接写 localStorage**」 | 她把 reducer 里的 8 处 `localStorage.setItem` 全搬到 Provider 的 `useEffect`。纯粹的工程重构，与产品口径无关，但改动面很大（`应用状态.tsx` 240 行）。 |
| 「完整发布岗位时保留角色专属计薪单位和全部筛选字段」 | `发布岗位` action 从 `{名称, 薪资带}` 改成 `{岗: 在招岗位}` 一次性派发。她的理由（审计文档原文）：「新岗位先创建半成品再补字段，存在编号与计薪单位错位」。 |

### 5.4 `e2e/onboarding.spec.ts`（`89f3f50` 建，`ef27340` + `8b30087` 扩充，共 7 个用例）

这是意图暴露得最彻底的地方 —— e2e 用例名就是她心里的三条用户路径：

- `walks the social-hire journey from role entry through both wizard stages`
- `walks the student journey from role entry to internship daily pay`
- `walks the recruiter journey from role entry to job posting`
- `migrates a legacy mixed job preference and keeps internship data on daily pay`
- `collects the campus graduation month separately from internship availability`
- `publishes an internship with explicit recruiter screening and process fields`
- `reuses the persisted portfolio link on the personal-strength page`

其中三条硬断言值得单独看：

1. `8b30087` 的招聘方用例：`await expect(page).toHaveURL(/#\/hr\/verify$/)` + `expect(page.getByRole('heading', { name: '实名认证' })).toBeVisible()` + `getByRole('button', { name: '开始人脸识别' }).click()` —— **她用 e2e 把"注册必过实名认证 + 人脸识别"钉成了浏览器级回归测试。**
2. `8b30087` 的社招用例：`expect(page.getByRole('button', { name: '已毕业' })).toHaveAttribute('aria-pressed', 'true')` —— 她把改写后的选项文案「已毕业」钉进了 e2e。想把文案改回「是/不是」，这条测试会红。
3. `ef27340` 的校招用例：填 `not-a-link` → 断言 `'请输入有效的作品集或项目链接'` 可见 + 「下一步」被 disable。**证实"可选"的作品集链接确实会阻断流程。**

### 5.5 `8b30087` 单独说 —— 这个提交是真 bug 修复

`8b30087` 只改了两个文件，`学生分流.tsx` 加了 3 行：

```
const 选身份 = (选了是: boolean) => {
+ // 已选中的身份再次被点击时不做任何事。此前每次点击都会重建默认筛选偏好，
+ // 用户只是确认当前选择，也会丢掉已经填写的实习日期、作品集和办公方式。
+ if (选了是 === 是学生) return;
```

原因是 `6dd3b15` 里她自己加的那行 `派发({ 型: '存求职筛选偏好', 偏好: 默认求职初筛偏好(选了是) })` 无条件重置。**这是她修她自己引入的回归，判定为真 bug 修复，与产品口径无冲突。** 配套的 e2e 断言写得很到位（填链接 → 重复点当前身份 → 断言链接仍在、办公方式仍是刚才选的）。

---

## 6. 分类汇总：「改坏了」vs「与你的口径不一致」

### A. 改坏了（技术缺陷）—— 1 条确认 + 2 条推测

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| A1 | 点击已选中的身份会清空所有已填偏好 | `6dd3b15` 引入，`8b30087` 已自修 | ✅ 已修 |
| A2 | `今天日期()` 用本地时区、`是真实日期()` 用 UTC，跨零点边界口径不一致 | `onboarding配置.ts:30` vs `:39` | **推测**，未复现 |
| A3 | `1c47b35` 新增的 dispatch-但-不跳转 路径与你 `51b6f20` 定位的向导白屏根因同源 | `引导问答.tsx` `下一题` | **推测**，未复现 |

`求职初筛缺失项` 把"缺项名"和"错误文案"混进同一个数组、导致轻提示读不通（第 3.4 节），严格说是体验瑕疵不是 bug，但属于同一类。

### B. 与你的口径不一致（更重要）—— 8 条，多数仍在 HEAD

| # | 冲突项 | 你的口径 | 她做了什么 | 证据 | 现状 |
|---|---|---|---|---|---|
| B1 | **对标物换了** | 只对标你给的 BOSS 截图 | 换成 LinkedIn + Indeed 的筛选字段模型 | `docs/onboarding与初筛审计-20260821.md:12-18` | 仍在 |
| B2 | **企业端实名认证** | 08-20 13:32：先不做，注册直进招聘名片 | 恢复认证前置，并写进合同 + 单测 + e2e 三层锁 | `6dd3b15` `选身份.tsx` / `onboarding配置.ts:身份首次入口` / `.test.ts:24` / `8b30087` e2e | **仍在** |
| B3 | **招聘名片可改姓名公司** | 08-20 13:35：认证已撤，公司也直接改 | 姓名/公司锁成 `已认证` 只读 | `6dd3b15` `招聘名片.tsx` | **仍在** |
| B4 | **分叉口问法** | 分叉口是「你是学生吗」，学生分支你自己出稿 | 改成「你目前是否在校？」+「在校/已毕业」，删掉"截图同款布局"注释 | `ab855a5` `学生分流.tsx:149,158` | **仍在**，且被 `8b30087` e2e 钉住 |
| B5 | **不加解释性小文案** | 「你就别加小文案了」 | 新增 4 处（2 处副行小字 + 2 处 placeholder）| `ab855a5` `学生分流.tsx:162-164,183`；`发布岗位.tsx` 两处 placeholder | **仍在** |
| B6 | **你定的文案** | 引导说明三步文案标注为「用用户给的表述」 | 整段重写 | `ab855a5` `引导说明.tsx` | 页面已被你 `51b6f20` 删除 |
| B7 | **代理替你谈的定位** | 双方各有 AI 代理替自己谈 | 全线把"谈判"改写成"匹配/初筛"，"匿名初筛合同"→"AI 初筛条件确认" | `ab855a5` + `89f3f50`，`披露偏好.tsx:29`、`披露说明.tsx:60,63`、`发布岗位.tsx:1133`、`岗位详情.tsx` | **仍在** |
| B8 | **截图之外的新字段** | 截图里没有、未经你确认的一律要单列确认 | 共 8 个求职端字段 + 5 个招聘端字段 + 9 个原默认值改必填 | 第 2 节全表 | **仍在** |

### C. 与你的口径一致（不必动）

- 双盲：`薪资初筛.ts` 只返回 `有交集/无交集/待核对`，且专门写了「不泄露候选人的具体预期」的防回归测试
- 两条求职路径必须不同：`onboarding配置.test.ts` 断言 `学生求职 ≠ 社招求职`
- onboarding 不带进度条：无回归
- 「待核对」≠「不符合」：审计文档写明后端约束，代码里落实
- `8b30087` 的偏好保护修复

---

## 7. 附：她的两份文档在仓库里的位置

两份都是 `6dd3b15` 新建，都还在 HEAD 上：

- `/Users/zhujiahao/agxp-a2a-recruiting-web/docs/onboarding与初筛审计-20260821.md`（59 行）—— **这份是理解她所有改动的钥匙**，「产品对照」+「三条本地流程」+「本轮发现并修复」+「后端接入约束」四节。你 `51b6f20` 动过它 4 行。
- `/Users/zhujiahao/agxp-a2a-recruiting-web/docs/代码与产品评审-20260821.md`（95 行）—— 通用代码评审，含一条 P0「薪资隐私口径冲突」（企业候选卡在意向确认前展示了匿名简历里的期望薪资带），这条是真发现，与双盲直接相关，值得单独核。

两份都没有进 `docs/INDEX.md`（该文件当前不存在）。
