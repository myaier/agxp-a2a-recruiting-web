# 同事 Alina Wu onboarding 改动 —— 依据来源取证

> 取证日期：2026-08-22 ｜ 范围：Alina Wu 的 6 个提交 + PR #1 ｜ 本轮只读不改

## 一句话结论

**有书面依据，但对标的不是产品负责人给的 BOSS直聘 截图。**
她在 `docs/onboarding与初筛审计-20260821.md:13-20` 写明依据是 **LinkedIn 和 Indeed 的帮助文档**（4 条带 URL 的引用），全文 **0 次**提到 BOSS。

---

## 1. 书面依据：有，且明确写出来了

### 1.1 唯一的依据段落

`docs/onboarding与初筛审计-20260821.md` 第 13-20 行，标题就叫「## 产品对照」，逐条列了 4 个来源：

| 行号 | 引用来源 | 她提取的规则 |
|---|---|---|
| :15 | LinkedIn Open to Work（带 help 链接） | 职位/地点/办公方式/开始时间/雇佣类型 做成独立偏好，不塞进自由文本 |
| :16 | LinkedIn Screening Questions（带 recruiter help 链接） | 学历/地点/语言/证书/经验/工作许可/技能 做成结构化问题，允许标 must-have |
| :17 | Indeed Job Search Preferences（带 support 链接） | 再拆出工作时间/薪资/通勤/异地/远程 |
| :18 | Indeed Screener Questions（带 howtohub 链接） | 明确区分 Required（可自动筛掉）与 Preferred（只排序提示） |

第 20 行是她自己的总结句，也是整套改动的设计原则：

> 「可借鉴的不是页面数量，而是"偏好 / 资格 / 必须项"三类数据不混用。」

这句话解释了她后面所有改动的动机——她做的是**初筛数据模型的分层**，不是**注册流程的版式模仿**。

### 1.2 依据里明确**没有**的东西

| 检查项 | 结果 | 证据 |
|---|---|---|
| 她的两个 doc 提到 BOSS | **0 次** | `grep -c -i boss` 对两个文件均返回 0 |
| 她引用产品负责人的 BOSS 基准文档 | **没有** | `docs/BOSS对照审计.md` 由 朱佳豪 于 871eeaf(08-18 18:39) 建立，她 6 个提交均未读取/引用/修改 |
| 她提到"截图" | **没有** | 她的 doc 无任何截图引用 |

### 1.3 代码里的 BOSS 注释 —— 全是产品负责人自己写的，不是她的依据

她的 diff 里 grep 到 BOSS 共 11 处，**全部是未改动的上下文行**，`git log -S` 溯源全部指向产品负责人：

| 注释原文 | 引入提交 | 作者 |
|---|---|---|
| `// 求职状态屏 /onboard/status（2026-08-20 按 BOSS 截图顺序重排）` | 3035efa (08-20 11:56) | 朱佳豪 |
| `// 非学生：先期望薪资，按 BOSS 截图序走` | 737990a (08-20 13:35) | 朱佳豪 |
| `/** 招聘类型（BOSS 对照补齐 2026-08-19）` | 74cae0a (08-18 22:22) | 朱佳豪 |
| `// 职位关键词（BOSS 对照补齐 2026-08-20）` | 8ce7133 (08-20 15:25) | 朱佳豪 |

**她自己新增的注释无一处提到 BOSS 或任何外部产品**，只讲内部业务逻辑（双盲、初筛合同、日薪/月薪单位）。

### 1.4 PR #1 与提交信息：无依据

- **PR #1 body**：只有「## 主要改动」5 条 + 「## 验证」4 条命令（typecheck/lint/test 16 项/build）。**无任何"参考了什么"的说明**。
- **PR 评论 / review**：`comments: []`、`reviews: []`，**全空**。
- **6 个提交的 body**：全部为空，只有标题行。

---

## 2. 从代码反推她在对标什么（**推测**）

### 2.1 主线：LinkedIn / Indeed 的结构化初筛数据模型（有书面佐证，非纯推测）

这条不是推测——她自己写在 doc 里了。代码与之高度自洽：
- `src/流程/onboarding配置.ts`：必备技能 vs 加分项分离，`/** 加分项只用于排序，不写入匿名初筛硬条件 */`（89f3f50）—— 就是 Indeed 的 Required/Preferred。
- `src/数据/薪资初筛.ts`：只返回区间是否相交（6dd3b15）。

### 2.2 支线：**推测** —— 部分字段来自"一个招聘产品应该有这些字段"的通用直觉

以下字段，在产品负责人的 `docs/BOSS对照审计.md` 里出现 **0 次**，在她自己引用的 LinkedIn/Indeed 条目里 **也没有对应描述**：

| 她新增的字段 | BOSS对照审计.md | 她自己的审计 doc | 出处 |
|---|---|---|---|
| 作品集链接 | 0 次 | 0 次 | 无书面来源 |
| 预计面试轮次 | 0 次 | 0 次 | 无书面来源 |
| 招聘节奏 | 0 次 | 0 次 | 无书面来源 |
| 是否提供转正机会 | 0 次 | 0 次 | 无书面来源 |
| 预计毕业月 | 0 次 | 0 次 | 无书面来源 |
| 必备技能 / 加分项 | 0 次 | 0 次 | 可归到 Indeed Required/Preferred |
| 每周到岗天数 | 0 次 | 3 次 | 仅她自己的 doc |
| 实习月数 | 0 次 | 1 次 | 仅她自己的 doc |
| 实习最晚开始日期 | 0 次 | 0 次 | 无书面来源 |

**推测**：面试轮次、招聘节奏、转正机会 这三项是国内实习/校招平台（实习僧、牛客一类）的常见字段，但**仓库里没有任何证据支持这个来源**，只能标为推测。更保守的说法是：这些来自"招聘产品通常有这些字段"的通用直觉。

---

## 3. AI 编码工具生成的迹象：证据较强

| 迹象 | 证据 |
|---|---|
| **分支名** | `codex/onboarding-screening-accessibility` —— OpenAI Codex 自动以 `codex/<slug>` 命名分支 |
| **PR 存活 41 秒** | createdAt `2026-08-21T06:12:38Z` → mergedAt `2026-08-21T06:13:19Z`，0 评论 0 review，自己合自己 |
| **提交信息风格断裂** | 首个提交 6dd3b15 + PR 标题/body 是中文；**后 5 个提交全部是英文祈使句单行、body 全空**（Streamline… / Align… / Expand… / Validate… / Protect…），典型 agent 生成风格。对比产品负责人的提交：全中文 + 带「用户/标注 + 日期」出处标签 |
| **同一 PR 内测试命名不一致** | e2e 用例全英文（`walks the social-hire journey from role entry through both wizard stages`），单测用例全中文（`招聘方首次注册必须先认证，再填写名片和岗位`）—— 同一个人同一轮改动出现两套语言习惯 |
| **重复提交** | 46233de 与 1c47b35 的 author/timestamp(08-21 15:09)/message/parent(8f1d0d5) 完全相同，SHA 不同 —— 同一 commit 被造了两次，rebase/重推痕迹 |
| **仓库无 agent 约定文件** | 无 AGENTS.md / CLAUDE.md / .codex*，agent 没有项目命名规范可循，可解释英文漂移 |

**结论**：分支名 + 41 秒自合 + 英文单行空 body 提交 + 中英测试命名分裂，四条独立证据指向同一结论——**这批改动基本可以确定由 AI 编码工具（Codex）生成**，她做的是发起与合并。

---

## 4. 与产品负责人口径的比对

区分「改坏了」和「和口径不一致」—— 本轮**没发现改坏的**，问题全在口径。

| 口径 | 判定 | 证据 |
|---|---|---|
| 1. onboarding 只对标 BOSS 截图 | **不一致** | 她的依据是 LinkedIn/Indeed（审计doc:13-18），BOSS 0 次；9 个新字段在 BOSS对照审计.md 里 0 命中 |
| 2. 学生分支产品负责人自己出稿 | **不一致** | 她在审计doc:24-28 定义了完整「学生求职」流程序列，并在 6dd3b15/ab855a5/ef27340/8b30087 中实现实习月数、每周到岗、毕业月、最晚开始日期。注：`src/屏幕/学生分流.tsx` 由产品负责人 c3394c8(08-19 12:49) 创建，她是**扩写**不是新建 |
| 3. 不自创解释性小文案 | **不一致** | ab855a5 引入多条，如「代理将按这些条件开始匹配和初筛」「写清楚岗位的职责和工作内容，候选人会在岗位详情中看到」「仅用于安排后续资料填写，不会限制你可选的机会类型」「先选一个主要方向，进入后可继续添加其他求职意向」。**产品负责人已在 13dc55b(08-21 18:21「按 BOSS 截图重做」) 改写了后两条**，说明已被察觉并部分回滚 |
| 4. onboarding 不要进度条 | **一致** | 全量 grep 她的新增行，无 progress / 进度条 / stepper / 「第N步共M步」。命中的「接洽进度」是页内 Tab 名，与 onboarding 无关 |
| 5. 两端同构 + 双盲不交换薪资数字 | **一致，且被强化** | `src/数据/薪资初筛.ts` 只返回交集布尔；单测「只返回薪资带是否相交，不泄露候选人的具体预期」；她还在 `docs/代码与产品评审-20260821.md` 主动标了 P0：企业候选卡违反双盲在读候选期望薪资带，要求产品拍板 |

---

## 5. 给产品负责人的三个待确认项

1. **依据换源要不要接受**：她把 onboarding 的对标物从 BOSS截图 换成了 LinkedIn/Indeed 的初筛数据模型。这个换源本身有合理性（她要解决的是"初筛字段无结构化来源"，BOSS截图确实不覆盖），但和「其他的先不用动」的指令冲突。
2. **学生分支**：她已经把学生流程实现了一版（实习月数/每周到岗/毕业月/日薪）。是采纳、还是等你自己出稿后推翻重画。
3. **小文案**：ab855a5 引入的解释性文案你已回滚 2 条，剩余若干条（如「代理将按这些条件开始匹配和初筛」）是否一并清掉。

---

## 附：证据命令

```
git log --format="%H%n%an%n%ad%n%B" <commit>          # 提交 body 全空
gh pr view 1 --json body,comments,reviews             # 41 秒自合，0 评论
grep -c -i boss docs/onboarding与初筛审计-20260821.md  # → 0
git log -S "按 BOSS 截图顺序重排" --format="%h %an"    # → 朱佳豪
git log -1 --format="%h parents=%p" 46233de 1c47b35   # 同 parent 不同 SHA
```
