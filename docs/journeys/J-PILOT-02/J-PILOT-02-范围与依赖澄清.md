# J-PILOT-02：求职者 onboarding 范围与依赖澄清

> 后续校准：当前产品规则见[旅程v1.0](J-PILOT-02-产品旅程与状态规则.md)，新增作品集／Highlights范围见[合同校准](J-PILOT-02-Highlights与作品集合同校准.md)。本报告保留初轮研究时态，旧待决策与交集结论不能覆盖后续确认的范围。

日期：2026-09-10。状态：范围已获用户确认；本文记录研究证据与待讨论项，尚非完整且已批准的产品旅程合同。未修改业务代码或 OpenAPI，未执行真实旅程。

后续产物：[产品旅程与状态规则 v1.0](J-PILOT-02-产品旅程与状态规则.md)，产品规则已确认，待实施。本文保留范围研究时态。

## 已确认范围

承接盘点 C02、C03及必要的身份入口与中断恢复：求职者建档 → 可选上传 PDF／手填 → 真实异步解析、建议回填与本人确认 → 基本信息、教育、经历、求职状态与偏好 → 首次意向保存 → 披露说明 → 头像 → 完成 onboarding 并进入首页。学生、社招作为同一旅程的分支。

用户明确：旅程按真实产品能力描述；测试可用 fixture，但不把模拟解析通过称为真实解析链路验收通过。用户另明确：通过大小周等卡片形成意向私有诉求是本旅程重要步骤，必须覆盖。

本次不进入显式委托、v3、S0、S1，不调整 J-PILOT-01 的业务终局、PDF版本绑定及当次投递授权。解析授权不能代替投递披露授权。

## 私有诉求：已存在的合同与未完成接线

本轮 fetch 后核对：FE `origin/main=40464f11a180791e919c473731cf4089d8d0343e`；BE `origin/release/0.2.5=1ae0b7a46f060e27c7f6db38e0469d15c626e2c5`。以下源码路径均相对相应 repo，应按上述 commit 读取。

- BE `docs/superpowers/plans/2026-09-10-recruitment-agent-task-continuous-negotiation.md:19`、`:275`：新 onboarding 排除卡片生成 `private_preferences`，四个历史 `exclusions` 均为 `unspecified`；旧历史数据不迁移。字段最多 10000 Unicode code points。一般偏好不能自动升级成硬性拒绝；意向 PATCH 是带当前 If-Match 的完整 payload 替换。
- 同一 plan `:281`：自由文本可能已有“不接受”等否定，显示时不能再次加“不”、取反或改变要求强度。示例文本是合同说明，不代表已经批准最终卡片拼接文案。
- FE `src/屏幕/引导问答.tsx` 的 `排除题`：包含“大小周”“纯外包 / 乙方”“全现场办公”“频繁出差”及自定义输入。当前标题为“哪些情况直接排除？”。
- FE `src/数据/后端映射.ts` 的 `转首次意向写入`：仍把部分内置选择转为 `excluded`，仅将判为自定义的文字写入 `private_preferences`。此外 UI 的合并标签“纯外包 / 乙方”与映射中的两个独立标签不同，后续需逐项核对，不能简单复制旧字符串分类。
- BE `apps/recruitment/internal/matchcase/recruitment_source.go:686` 的 `renderPrivatePreference`：将类型化意向与私有文本构成候选私有材料；`matchcase/projection.go:151` 将该材料带入候选条件确认 projection。不能据此宣称真实模型已经正确理解所有具体诉求。

结论：此项是已有后端合同下的前端语义接线缺口；不应重设计一套规则引擎，也不自动等同于独立 Agent 长期规则提案流程。

## 与 J-PILOT-01 的隔离和交接

01 的合同候选为 BE `264829174`，产品规则见[已批准旅程](../J-PILOT-01/J-PILOT-01-产品旅程与状态规则.md)。

02 负责“用户表达 → 忠实构造私有诉求 → 保存首次意向 → 权威回读与恢复”；01 负责后续委托与条件确认消费该意向。共同引用 release 已有的 `private_preferences` 语义，02 不修改 01 的 Negotiation、MatchCase 和委托合同。

上一轮初查的 onboarding 核心接口及其 Schema 引用未命中 01 的合同改动；这不等于所有代码或依赖完全独立。意向映射、附件库、会话水合是共享实现区域，正式实施前应按最终范围重做影响核对。01 使用按既有合同准备的意向和附件即可独立验收，不等待02 UI；02 不以S0通过作为自己的完成条件。

## 后续草稿应解决的具体问题

1. 内置卡片如何准确生成要求、自定义文本如何保留原意；何处让用户核对最终保存的意思，是否需要额外摘要展示尚未决定，不自行新增确认页。
2. 未选择与明确排除的区别；硬性要求与一般偏好如何表达；自定义矛盾或含否定文本不能擅自改写。
3. 刷新、返回修改、首次保存结果未知和中断重入时如何避免丢失、重复拼接或重复创建意向。
4. 同页“屏蔽公司”属于独立隐私行为：当前 UI 有本地状态，需继续核实持久化与身份映射，不能只拼入私有文本便宣称真实屏蔽。
5. PDF 解析等待、失败、继续手填、迟到结果保护；已有多份附件时重入 onboarding 的选择／替换规则。
6. onboarding 完成与开始推荐的真实边界；当前定时“扫描862个岗位”动画不能作为后端完成证据。

下一位 Agent：基于本范围和最新两仓版本编写产品旅程与状态规则草稿；正常路径、异步、失败、恢复、角色权限、私有诉求形成与保存均须有来源及验收条件。未确认行为单列待决策。先设计与讨论，不实施；fixture 验证与真实解析、真实模型证据分别记录。
