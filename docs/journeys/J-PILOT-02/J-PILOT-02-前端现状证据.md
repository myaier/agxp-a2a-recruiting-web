# J-PILOT-02 前端现状研究

日期2026-09-10。源码基线 `origin/main@40464f11a180791e919c473731cf4089d8d0343e`，git archive 导出 `/tmp/pilot02-fe` 阅读；所有路径行号针对该SHA。已读CLAUDE。未运行测试/浏览器/API；研究时未修改业务源码。用户确认完整真实onboarding，fixture仅测试手段。

## 主线及落盘

流程权威当前代码 `src/流程/onboarding配置.ts:108`：学生：完善资料→基本信息→学历→学校→专业→就读时间→经历→求职状态→偏好向导→披露→头像→首页（当前另经过初始化动画）。社招：完善资料→薪资向导→基本信息→求职状态→学历→学校→专业→时间→经历→偏好向导→披露→头像→首页。社招向导两段URL显式区分，不依赖薪资是否已答推测题序（同文件:193、208）。

| 环节 | 实现事实 | 证据 |
|---|---|---|
| 身份/偏好/目录 | Backend初始身份三态、求职/办公选择为空，用户明确选一个主类型；校招毕业月、实习月数/天数条件必填；城市职位需目录引用。先草稿，最后创建意向 | `src/屏幕/学生分流.tsx:103`、264；`src/流程/onboarding配置.ts:254`、258 |
| PDF | 预检→授权→真实上传；不上传可手填。空库创建、非空替换items[0]，并非自动挑选最优简历 | `src/屏幕/学生分流.tsx:48`、163、184 |
| 基本信息 | 学生身份可写，社招身份尚空时只Context暂存，去状态页选完统一写 | `src/屏幕/基本信息.tsx:179-205`；`src/屏幕/求职状态.tsx:45-73` |
| 学历/学校/专业/时间 | 每页调用保存简历，成功才确认预填分区。但不完整教育条目跳过服务端写入，不能将“点击下一步成功”概括为每个选择均已落盘 | `最高学历.tsx:58-67`；`毕业院校.tsx:146-163`；`选专业.tsx:142-159`；`就读时间段.tsx:64-87`（均src/屏幕） |
| 在线简历 | 工作/教育/技能/证书先建议，补目录和必填；分区diff多请求，非事务；中途失败权威回读 | `src/屏幕/工作经历.tsx:111-123`、166-206；`src/数据/招聘数据源/简历.ts:65-112` |
| 私有诉求/个人优势 | 卡片仅useState；最后先保存summary再首次意向，summary成功独立确认 | `src/屏幕/引导问答.tsx:98-100`、150-188 |
| 披露 | 纯静态内容，按钮只跳头像，无匹配/授权命令 | `src/屏幕/披露说明.tsx:16-71` |
| 头像/完成 | Backend真实上传JPG/PNG≤10MiB，可不上传直接完成；完成只清预填轮/切Tab/导航 | `src/屏幕/添加头像.tsx:16-64`、97 |
| 初始化 | 固定862扫描数、3.6s动画，无真实任务状态 | `src/屏幕/初始化页.tsx:23-31`、60-90 |

## PDF预填已真实接线，旧handoff不能误作禁预填

9/1旧handoff已被9/3设计及后续实现演进。`docs/superpowers/specs/2026-09-03-candidate-onboarding-resume-prefill-frontend-design.md:19`说明只读建议和逐页应用；该历史文件头仍写草案，不宜将其视为整条旅程的已确认合同，但当前源码已实现。

`src/屏幕/学生分流.tsx:48-88`以权威file/version/parse坐标推进，仅在本页明确上传/替换激活；旧附件直接进入不激活。`:282-304`解析在途离页问再等等/继续手填，手填只禁止迟到预填，不取消后端解析。`src/状态/后端/简历预填操作.ts:191-235`单飞和主体/角色/会话/source fence；`:253-305`404/409一次权威刷新；`:357-416`恢复（消费页无poller且仍pending则转manual）；`:442-466`显式重试/手填。实际GET `resume-files/{file_id}/parse-result?version_id=...&parse_id=...`见`src/数据/招聘数据源/简历预填.ts`。

建议只作空白初值，身份不猜，已有值优先，非空列表不下标合并（`src/流程/候选Onboarding简历预填.ts:76`及各页映射）。`src/数据/候选Onboarding预填恢复.ts:1-14`只session存mode/source/eligibility/confirmed/generation，正文不缓存，刷新exact tuple重取；`src/流程/候选Onboarding预填边界.tsx:82-134`读完再挂页。它不是完整onboarding草稿恢复。

## 初步缺口与产品待决策

### F02-01 private_preferences语义冲突（已确认需要改）

`src/数据/后端映射.ts:377-404`当前大小周/现场/出差仍写exclusions=excluded，仅自定义写private_preferences。与用户确认新onboarding四个旧字段unspecified、统一私有诉求不同。具体拼接、硬要求与一般偏好、否定输入和用户核对方式需草稿明确，不再询问是否要接这项。

额外现有bug：`引导问答.tsx:1106`卡名“纯外包 / 乙方”，映射内置却只有“纯外包”“乙方”，整张卡当前落自定义文本。`:1143`“哪些情况直接排除？”及`:98`直接挡掉不来问人的注释不应继续当合同。

### F02-02 私有诉求及屏蔽公司恢复/落盘缺口

卡片`引导问答.tsx:100`为useState([])，未进`src/数据/资料缓存.ts:237-240`草稿白名单。刷新或卸载向导丢失，已有意向也不回填卡片。

屏蔽公司`引导问答.tsx:1108-1138`全部子组件useState，社招默认简历公司名、学生默认关；无组织ID、无真实API调用、无持久化、无首次意向payload。已有真实隐私能力`src/状态/后端/隐私操作.ts:159-199`不代表onboarding接线。保留此可见能力需确认默认规则和组织消歧；不能用private_preferences文字代替组织屏蔽。

### F02-03 恢复部分支持

`src/数据/资料缓存.ts:217-240`白名单session存城市/职位/引用、筛选、薪资、到岗、学生选择，按模式环境主体隔离；不存排除与简历正文。`src/状态/资料持久化.ts:48-76`完整简历仅Mock持久化。

`src/数据/招聘数据源/简历.ts:96-101`明确不完整教育/经历不发请求，只本地留存，刷新或水合会清掉。因此学校→专业→时间等多屏及社招未定身份基本信息存在丢失窗口。先定义恢复范围，不要擅自把敏感正文全写localStorage。

### F02-04 首次意向未知结果与返回修改

`src/状态/后端/候选操作.ts:290-314`真实列表非空直接no-op，空才创建；首次成功后回退改卡片再保存依然no-op，不更新已有意向，也不证明其属于本轮。内存写锁只保护在途。

`src/数据/招聘数据源/意向.ts:47-57`POST成功后GET列表成功才返回。`src/数据/HTTP客户端.ts:290-303`每次新调用随机新key，单调用409/503有限受控重试复用（:325-340）。`src/状态/后端/候选操作.ts:100-129`409/503重读列表继续抛错；status0网络错误无同等对账。POST成功GET失败/刷新重试没有持久命令关联。需定义重复创建保护及恢复，不以空列表证明未接收。

附件未知结果已有不同处理：`src/状态/后端/附件简历操作.ts:176-218`create/replace回读集合变化仅报“附件状态已更新，请确认”，不冒认本机成功；不要概括所有写入都无对账。

### F02-05 完成与登录恢复

`src/应用.tsx:241-251`登录恢复candidate直接首页，不判断onboarding完成或恢复步骤；`添加头像.tsx:27-35`只有导航，没有完成回执/最终资料检查。头像可跳过是现状待产品明确。披露页“完成设置，开始匹配”“开始匹配和初筛”与定时862没有真实执行证据，02应定义资料意向保存后进首页，不触及01显式委托，不为假进度新增扫描API。

### F02-06 顶层作品集链接

`工作经历.tsx:184`、`引导问答.tsx:150-151`独立本地派发；`src/数据/招聘数据源/简历.ts`无该顶层字段mutation，`资料持久化.ts:48-59`仅Mock存。需核对产品归属，不把已有经历projects API误作该UI已接线。

## 现有验证支持（本次未运行）

`e2e/onboarding.spec.ts:42`社招、:86学生、:178校招是Mock主线，候选核心用例到披露，不证明真实完整到首页。`src/状态/后端/简历预填操作.test.ts:284`起覆盖pending/succeeded/单飞/source替换/角色主体迟到/manual/401/404/409恢复；`src/数据/候选Onboarding预填恢复.test.ts`、`流程/候选Onboarding预填边界.test.tsx`、`流程/候选Onboarding简历预填.test.ts`、各页测试及`状态/应用状态.test.ts:3129`session草稿可复用。`数据/招聘数据源/简历预填.fixture.ts`是fixture，不是模型解析实测。历史`docs/dogfood/backend-local-onboarding.md`、`docs/runs/2026-09-01-backend-local-onboarding-dogfood.md`不是当前SHA通过证据。

## 分支与01隔离

已获取refs里`origin/onboarding-screening-accessibility@46233ded162a6b05061241663b215444061c067f`比main有1独有提交，早期身份入口/日薪改动；main已具备后续冻结题序/日薪修复，不能把三点diff当当前净补丁，没有新private_preferences接线。`origin/fe-backend-mock-fixes@22c3be71`独有设置页设计文档；其他环境/验收refs独有0，gh-pages为发布产物。只覆盖本地remote refs，未宣称全部远端PR审查。

02可限页面/首次意向映射/预填草稿/完成语义，不改Negotiation/MatchCase业务状态；附件共享库items[0]替换策略须讨论，不能破坏01冻结file/version。`应用状态.tsx`、`后端映射.ts`共享文件小块协调。02验收使用独立新候选，不依赖01实施或初筛结果。

### 回读路径与教育保存补充核实

前端`src/数据/招聘数据源/简历.ts:221-222`读取聚合`GET /api/v1/me/resume`，同文件:83只对`/resume/profile`执行PATCH；没有另用GET resume/profile作为整条建档回读。保存过程:204失败后GET权威，:208成功后GET最终；:210-218将本次跳过的不完整教育/经历合回返回页面态，但不进入服务端快照。学历可以先写profile.current_education；学校页:152与专业页:148虽都调用保存，教育条目若缺学校/专业/开始日期仍不POST；时间补齐后才有完整教育POST（数据源:155-170）。预填若一开始给足字段，则可能更早成为完整条目，不能一概断言只有时间页会写。

salary确实属于`资料缓存.ts:228`草稿并可session恢复；排除项不属于白名单且只useState，恢复能力不同。首次意向成功后返回改卡片再次点击，列表非空no-op已经源码确认。
