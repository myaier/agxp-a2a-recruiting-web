// 招聘在谈卡：Mock 在谈候选卡与 Backend P5 招聘在谈卡共用的唯一卡面（Plan 公共展示契约 /
// Spec §5.2）。视觉基准 = 原 企业在谈候选.tsx 的 Mock 候选卡：候选信息主体（头行 → 工作/
// 教育图标行 → 亮点标签行）在标签下方接 在谈阶段区；右侧始终是匹配分位 —— Backend 的
// P5 open 无匹配分，由调用方传 null，走 卡片分数 的未知占位（不补 0、不画环）。
// 整卡点击 = 打开（本 Case 详情坐标由各连接层给）；在谈卡不新增收藏/委托/淘汰按钮，
// 待办/徽标表达全在 阶段区（在谈阶段信息），卡内不写 mock/backend 判断。
// 不 import Context/fixture/HTTP/路由/持久化，不派发、不请求、不算分。
import { 白卡 } from '../通用';
import 候选信息主体 from './候选信息主体';
import 卡片分数 from './卡片分数';
import 样式 from './招聘在谈卡.module.css';
import 在谈阶段区 from './在谈阶段区';
import type { 招聘在谈卡属性 } from './类型';

export default function 招聘在谈卡({ 信息, 匹配分, 阶段, 打开 }: 招聘在谈卡属性) {
  return (
    <div className={样式.根} data-testid="招聘在谈卡">
      {/* 整卡一个 button（白卡 + 按下）→ 详情由连接层给坐标。分数位放主体前：
          阅读顺序 = 头行 + 右侧匹配分 一组 → 工作 → 教育 → 标签 → 阶段区（Spec §5.2）；
          候选信息主体 / 在谈阶段区 的签名冻结，分数位只能由卡层绝对定位，
          落点仍是头行右侧同一位置。 */}
      <白卡 按下={打开} 类名={样式.卡}>
        <div className={样式.右列} data-card-region="score">
          <卡片分数 分={匹配分} />
        </div>
        <候选信息主体 信息={信息} />
        <在谈阶段区 信息={阶段} />
      </白卡>
    </div>
  );
}
