// 简历行业选择正文（picker 统一 Task 1）：经历编辑页 所属行业 底部选择层的两模式
// 共用正文。版式照原页行业层 JSX 自上而下：抓手 → 标题「所属行业」→ 行业折叠目录列表
// （共用 行业分类列表）。样式直接沿用原页面 CSS（工作经历.module.css 的
// 遮罩/选择层/选择层抓手/选择层标题），不另建弹层样式、不为层级新造图标/面包屑。
//
// 原页 Mock 的 自填 自由文本输入按 Plan 删除（两模式无「自填行业」，Backend 本就不
// 提供自由文本）；目录展开/缓存/分页/重试/换代机制由 屏幕/行业目录钩子 提供，页面注入
// 查询适配。本组件是纯展示正文：只收目录状态、稳定键、已选与回调，不依赖数据源模式、
// BFF DTO、Context、API、路由或存储；选定归属（单选写当前经历草稿并关闭）在页面外层。
//
// 面板高度：弹层框架不再内联 maxHeight:'none'，caller（选择层）的 72% 限高生效，
// 列表容器（行业分类列表 .列表）flex:1 / min-height:0 / overflow-y:auto 内部滚动。

import 样式 from '../屏幕/工作经历.module.css';
import 弹层框架 from './弹层框架';
import { 行业分类列表, type 行业项, type 行业目录状态 } from './行业分类列表';

/** 正文 props（页面局部契约，不是公共领域模型）；单选（上限=1） */
export type 简历行业选择正文Props = {
  目录: 行业目录状态;
  已选键: readonly string[];
  选择: (项: 行业项) => void;
  关闭: () => void;
};

export function 简历行业选择正文({ 目录, 已选键, 选择, 关闭 }: 简历行业选择正文Props): React.JSX.Element {
  return (
    <弹层框架 标签="选择所属行业" 遮罩类名={样式.遮罩} 面板类名={样式.选择层} 关闭={关闭}>
      <div className={样式.选择层抓手} />
      <div className={样式.选择层标题}>所属行业</div>
      <行业分类列表 {...目录} 已选键={已选键} 上限={1} 选择={选择} />
    </弹层框架>
  );
}