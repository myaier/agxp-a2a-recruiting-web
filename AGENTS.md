# AGENTS.md

## 项目指引

本仓库的通用 AI agent 指引以 [CLAUDE.md](CLAUDE.md) 为主要真相源。Codex 等读取本文件的 agent 在开始工作前，必须完整阅读并遵守 `CLAUDE.md`。

为确保 Claude Review、Codex Review 等只读取其中一个入口时也能获得关键约束，「工程原则」「Spec / Plan 工程约束」「Code Review Rules」在 `CLAUDE.md` 与 `AGENTS.md` 中刻意双写，修改时必须同步；其他共享规则只维护在 `CLAUDE.md`，仅适用于 Codex 的补充规则可以追加在本文件中。

## 工程原则

- 保持简单。解决当前问题，而不是想象中的未来问题。
- 优先采用能够解决问题的最小改动。
- 避免过早抽象。只有在真实用例反复出现后才进行抽象。
- 没有证据，不要增加基础设施或复杂度。
- 先构建最小可行产品（MVP），测量后再优化。

## Spec / Plan 工程约束

- Spec 和 plan 必须遵守本文件的工程原则，并把它们具体化为本任务的设计取舍，而不是机械复制原则清单。
- 说明为什么所选方案是满足当前需求的最小方案。
- 明确非目标、刻意延后的能力，以及未来重新考虑它们所需的证据。
- 引入新的抽象、基础设施、配置项或通用机制时，说明对应的当前用例或现实风险；没有依据则不引入。
- Plan 不得在已批准 spec 之外自行增加架构层次、通用化工作或“顺便”重构。

## Code Review Rules

- 审查前先阅读并遵守本文件的「工程原则」，以及变更对应的 spec / plan。
- Review 的目标包括简化设计、删除不必要复杂度和保持代码整洁，不只是发现 correctness 问题。
- 不要为了假想需求、理论完备性或未来扩展性要求增加抽象、基础设施、配置项或通用机制。
- 任何会增加复杂度的 finding，必须指出它防止的现实故障、对应的当前需求或已有证据；否则优先建议简化，或不作为 finding。
- 每条 finding 必须标注 `必要性：required | optional` 和 `复杂度影响：降低 | 不变 | 增加`；标为 `增加` 时必须说明收益为何足以抵偿新增的维护与认知成本。
- 区分真实缺陷与可选增强；可选增强不得作为必须修复的 finding。
- 接收 review 的 agent 必须逐条核实 finding，不得盲目采纳；对于违反本文件规则、缺乏证据或收益不足以抵偿复杂度的建议，有权且有责任拒绝，并简要记录理由。
- 可以主动建议删除代码、合并层次、内联一次性抽象和复用现有机制。
