# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
test-results
playwright-report
*.local

# Capacitor 生成的原生工程（按方案第 4 步接壳后产生；是否入库看团队约定，
# 先忽略以免原型阶段把 Xcode 工程噪音带进历史）
/ios
/android

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
标注收件箱.jsonl
.标注端点

# UI 回归编排输出（参考/候选采集与报告）
ui-regression-output/

# agent-browser 真实后端整栈验收的运行产物（候选截图、diff、报告、日志）
/agent-browser-backend-output/

# Agent dogfood 临时证据（截图、视频、HAR、Chrome profile、会话状态与原材料副本）
dogfood-output/
